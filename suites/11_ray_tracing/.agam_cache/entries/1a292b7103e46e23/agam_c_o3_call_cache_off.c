/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_max_val(agam_int __p0, agam_int __p1);
agam_int agam_min_val(agam_int __p0, agam_int __p1);
agam_int agam_bvh_traversal(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_max_val(agam_int __p0, agam_int __p1) {
  agam_int _local_a = __p0;
  agam_int _local_b = __p1;
block_2:
  agam_int __v5 = _local_a;
  agam_int __v6 = _local_b;
  agam_bool __v7 = __v5 > __v6;
  if (__v7) goto block_3; else goto block_5;
block_3:
  agam_int __v8 = _local_a;
  return __v8;
block_5:
  agam_int __v9 = _local_b;
  return __v9;
}

agam_int agam_min_val(agam_int __p0, agam_int __p1) {
  agam_int _local_a = __p0;
  agam_int _local_b = __p1;
block_8:
  agam_int __v13 = _local_a;
  agam_int __v14 = _local_b;
  agam_bool __v15 = __v13 < __v14;
  if (__v15) goto block_9; else goto block_11;
block_9:
  agam_int __v16 = _local_a;
  return __v16;
block_11:
  agam_int __v17 = _local_b;
  return __v17;
}

agam_int agam_bvh_traversal(agam_int __p0) {
  agam_int _local_rays = __p0;
block_14:
  agam_int _local_nodes = 0;
  agam_int __v20 = 0;
  agam_int __v22 = 256;
  _local_nodes = __v22;
  agam_int __v23 = __v22;
  agam_int _local_checksum = 0;
  agam_int __v24 = 0;
  agam_int __v26 = 0;
  _local_checksum = __v26;
  agam_int __v27 = __v26;
  agam_int _local_r = 0;
  agam_int __v28 = 0;
  agam_int __v30 = 0;
  _local_r = __v30;
  agam_int __v31 = __v30;
  goto block_15;
block_15:
  agam_int __v32 = _local_r;
  agam_int __v33 = _local_rays;
  agam_bool __v34 = __v32 < __v33;
  if (__v34) goto block_16; else goto block_17;
block_16:
  agam_int _local_ox = 0;
  agam_int __v35 = 0;
  agam_int __v36 = _local_r;
  agam_int __v38 = 17;
  agam_int __v39 = __v36 * __v38;
  agam_int __v41 = 1000;
  agam_int __v42 = __v39 % __v41;
  _local_ox = __v42;
  agam_int __v43 = __v42;
  agam_int _local_oy = 0;
  agam_int __v44 = 0;
  agam_int __v45 = _local_r;
  agam_int __v47 = 31;
  agam_int __v48 = __v45 * __v47;
  agam_int __v50 = 1000;
  agam_int __v51 = __v48 % __v50;
  _local_oy = __v51;
  agam_int __v52 = __v51;
  agam_int _local_dx = 0;
  agam_int __v58 = 0;
  agam_int __v59 = _local_r;
  agam_int __v61 = 13;
  agam_int __v62 = __v59 * __v61;
  agam_int __v64 = 20;
  agam_int __v65 = __v62 % __v64;
  agam_int __v67 = 10;
  agam_int __v68 = __v65 - __v67;
  _local_dx = __v68;
  agam_int __v69 = __v68;
  agam_int _local_dy = 0;
  agam_int __v70 = 0;
  agam_int __v71 = _local_r;
  agam_int __v73 = 19;
  agam_int __v74 = __v71 * __v73;
  agam_int __v76 = 20;
  agam_int __v77 = __v74 % __v76;
  agam_int __v79 = 10;
  agam_int __v80 = __v77 - __v79;
  _local_dy = __v80;
  agam_int __v81 = __v80;
  agam_int _local_stack_size = 0;
  agam_int __v86 = 0;
  agam_int __v88 = 0;
  _local_stack_size = __v88;
  agam_int __v89 = __v88;
  agam_int _local_current_node = 0;
  agam_int __v90 = 0;
  agam_int __v92 = 0;
  _local_current_node = __v92;
  agam_int __v93 = __v92;
  agam_int _local_hits = 0;
  agam_int __v94 = 0;
  agam_int __v96 = 0;
  _local_hits = __v96;
  agam_int __v97 = __v96;
  agam_int _local_steps = 0;
  agam_int __v98 = 0;
  agam_int __v100 = 0;
  _local_steps = __v100;
  agam_int __v101 = __v100;
  goto block_18;
block_18:
  agam_int __v102 = _local_steps;
  agam_int __v104 = 100;
  agam_bool __v105 = __v102 < __v104;
  if (__v105) goto block_19; else goto block_20;
block_19:
  agam_int _local_min_x = 0;
  agam_int __v106 = 0;
  agam_int __v107 = _local_current_node;
  agam_int __v109 = 7;
  agam_int __v110 = __v107 * __v109;
  agam_int __v112 = 1000;
  agam_int __v113 = __v110 % __v112;
  _local_min_x = __v113;
  agam_int __v114 = __v113;
  agam_int _local_max_x = 0;
  agam_int __v115 = 0;
  agam_int __v116 = _local_min_x;
  agam_int __v118 = 100;
  agam_int __v119 = __v116 + __v118;
  _local_max_x = __v119;
  agam_int __v120 = __v119;
  agam_int _local_min_y = 0;
  agam_int __v121 = 0;
  agam_int __v122 = _local_current_node;
  agam_int __v124 = 11;
  agam_int __v125 = __v122 * __v124;
  agam_int __v127 = 1000;
  agam_int __v128 = __v125 % __v127;
  _local_min_y = __v128;
  agam_int __v129 = __v128;
  agam_int _local_max_y = 0;
  agam_int __v130 = 0;
  agam_int __v131 = _local_min_y;
  agam_int __v133 = 100;
  agam_int __v134 = __v131 + __v133;
  _local_max_y = __v134;
  agam_int __v135 = __v134;
  agam_int _local_tmin_x_raw = 0;
  agam_int __v136 = 0;
  agam_int __v137 = _local_min_x;
  agam_int __v138 = _local_ox;
  agam_int __v139 = __v137 - __v138;
  agam_int __v141 = 100;
  agam_int __v142 = __v139 * __v141;
  agam_int __v143 = _local_dx;
  agam_int __v145 = 1;
  agam_int __v146 = __v143 + __v145;
  agam_int __v147 = __v142 / __v146;
  _local_tmin_x_raw = __v147;
  agam_int __v148 = __v147;
  agam_int _local_tmax_x_raw = 0;
  agam_int __v149 = 0;
  agam_int __v150 = _local_max_x;
  agam_int __v151 = _local_ox;
  agam_int __v152 = __v150 - __v151;
  agam_int __v154 = 100;
  agam_int __v155 = __v152 * __v154;
  agam_int __v156 = _local_dx;
  agam_int __v158 = 1;
  agam_int __v159 = __v156 + __v158;
  agam_int __v160 = __v155 / __v159;
  _local_tmax_x_raw = __v160;
  agam_int __v161 = __v160;
  agam_int _local_tmin_x = 0;
  agam_int __v162 = 0;
  agam_int __v163 = _local_tmin_x_raw;
  agam_int __v164 = _local_tmax_x_raw;
  agam_int __v165 = agam_min_val(__v163, __v164);
  _local_tmin_x = __v165;
  agam_int __v166 = __v165;
  agam_int _local_tmax_x = 0;
  agam_int __v167 = 0;
  agam_int __v168 = _local_tmin_x_raw;
  agam_int __v169 = _local_tmax_x_raw;
  agam_int __v170 = agam_max_val(__v168, __v169);
  _local_tmax_x = __v170;
  agam_int __v171 = __v170;
  agam_int _local_tmin_y_raw = 0;
  agam_int __v172 = 0;
  agam_int __v173 = _local_min_y;
  agam_int __v174 = _local_oy;
  agam_int __v175 = __v173 - __v174;
  agam_int __v177 = 100;
  agam_int __v178 = __v175 * __v177;
  agam_int __v179 = _local_dy;
  agam_int __v181 = 1;
  agam_int __v182 = __v179 + __v181;
  agam_int __v183 = __v178 / __v182;
  _local_tmin_y_raw = __v183;
  agam_int __v184 = __v183;
  agam_int _local_tmax_y_raw = 0;
  agam_int __v185 = 0;
  agam_int __v186 = _local_max_y;
  agam_int __v187 = _local_oy;
  agam_int __v188 = __v186 - __v187;
  agam_int __v190 = 100;
  agam_int __v191 = __v188 * __v190;
  agam_int __v192 = _local_dy;
  agam_int __v194 = 1;
  agam_int __v195 = __v192 + __v194;
  agam_int __v196 = __v191 / __v195;
  _local_tmax_y_raw = __v196;
  agam_int __v197 = __v196;
  agam_int _local_tmin_y = 0;
  agam_int __v198 = 0;
  agam_int __v199 = _local_tmin_y_raw;
  agam_int __v200 = _local_tmax_y_raw;
  agam_int __v201 = agam_min_val(__v199, __v200);
  _local_tmin_y = __v201;
  agam_int __v202 = __v201;
  agam_int _local_tmax_y = 0;
  agam_int __v203 = 0;
  agam_int __v204 = _local_tmin_y_raw;
  agam_int __v205 = _local_tmax_y_raw;
  agam_int __v206 = agam_max_val(__v204, __v205);
  _local_tmax_y = __v206;
  agam_int __v207 = __v206;
  agam_int _local_tmin = 0;
  agam_int __v208 = 0;
  agam_int __v209 = _local_tmin_x;
  _local_tmin = __v209;
  agam_int __v210 = __v209;
  agam_int __v211 = _local_tmin_y;
  agam_int __v212 = _local_tmin;
  agam_bool __v213 = __v211 > __v212;
  if (__v213) goto block_21; else goto block_23;
block_21:
  agam_int __v214 = _local_tmin_y;
  _local_tmin = __v214;
  agam_int __v215 = __v214;
  goto block_23;
block_23:
  agam_int _local_tmax = 0;
  agam_int __v216 = 0;
  agam_int __v217 = _local_tmax_x;
  _local_tmax = __v217;
  agam_int __v218 = __v217;
  agam_int __v219 = _local_tmax_y;
  agam_int __v220 = _local_tmax;
  agam_bool __v221 = __v219 < __v220;
  if (__v221) goto block_24; else goto block_26;
block_24:
  agam_int __v222 = _local_tmax_y;
  _local_tmax = __v222;
  agam_int __v223 = __v222;
  goto block_26;
block_26:
  agam_int __v224 = _local_tmax;
  agam_int __v225 = _local_tmin;
  agam_bool __v226 = __v224 >= __v225;
  agam_int __v227 = _local_tmax;
  agam_int __v229 = 0;
  agam_bool __v230 = __v227 > __v229;
  agam_bool __v231 = __v226 && __v230;
  if (__v231) goto block_27; else goto block_28;
block_27:
  agam_int _local_is_leaf = 0;
  agam_int __v232 = 0;
  agam_int __v233 = _local_current_node;
  agam_int __v235 = 3;
  agam_int __v236 = __v233 % __v235;
  agam_int __v238 = 0;
  agam_bool __v239 = __v236 == __v238;
  _local_is_leaf = __v239;
  agam_int __v240 = __v239;
  agam_int __v241 = _local_is_leaf;
  agam_int __v243 = 0;
  agam_bool __v244 = __v241 != __v243;
  if (__v244) goto block_30; else goto block_31;
block_30:
  agam_int __v245 = _local_hits;
  agam_int __v247 = 1;
  agam_int __v248 = __v245 + __v247;
  _local_hits = __v248;
  agam_int __v249 = __v248;
  agam_int __v250 = _local_stack_size;
  agam_int __v252 = 1;
  agam_int __v253 = __v250 - __v252;
  _local_stack_size = __v253;
  agam_int __v254 = __v253;
  agam_int __v255 = _local_stack_size;
  agam_int __v257 = 0;
  agam_bool __v258 = __v255 < __v257;
  if (__v258) goto block_33; else goto block_35;
block_33:
  agam_int __v260 = 999;
  _local_steps = __v260;
  agam_int __v261 = __v260;
  goto block_35;
block_35:
  agam_int __v262 = _local_current_node;
  agam_int __v264 = 2;
  agam_int __v265 = __v262 * __v264;
  agam_int __v266 = _local_nodes;
  agam_int __v267 = __v265 % __v266;
  _local_current_node = __v267;
  agam_int __v268 = __v267;
  goto block_32;
block_31:
  agam_int __v269 = _local_stack_size;
  agam_int __v271 = 1;
  agam_int __v272 = __v269 + __v271;
  _local_stack_size = __v272;
  agam_int __v273 = __v272;
  agam_int __v274 = _local_current_node;
  agam_int __v276 = 2;
  agam_int __v277 = __v274 * __v276;
  agam_int __v279 = 1;
  agam_int __v280 = __v277 + __v279;
  agam_int __v281 = _local_nodes;
  agam_int __v282 = __v280 % __v281;
  _local_current_node = __v282;
  agam_int __v283 = __v282;
  goto block_32;
block_32:
  goto block_29;
block_28:
  agam_int __v284 = _local_stack_size;
  agam_int __v286 = 1;
  agam_int __v287 = __v284 - __v286;
  _local_stack_size = __v287;
  agam_int __v288 = __v287;
  agam_int __v289 = _local_stack_size;
  agam_int __v291 = 0;
  agam_bool __v292 = __v289 < __v291;
  if (__v292) goto block_36; else goto block_38;
block_36:
  agam_int __v294 = 999;
  _local_steps = __v294;
  agam_int __v295 = __v294;
  goto block_38;
block_38:
  agam_int __v296 = _local_current_node;
  agam_int __v298 = 1;
  agam_int __v299 = __v296 + __v298;
  agam_int __v300 = _local_nodes;
  agam_int __v301 = __v299 % __v300;
  _local_current_node = __v301;
  agam_int __v302 = __v301;
  goto block_29;
block_29:
  agam_int __v303 = _local_steps;
  agam_int __v305 = 1;
  agam_int __v306 = __v303 + __v305;
  _local_steps = __v306;
  agam_int __v307 = __v306;
  goto block_18;
block_20:
  agam_int __v308 = _local_checksum;
  agam_int __v310 = 31;
  agam_int __v311 = __v308 * __v310;
  agam_int __v312 = _local_hits;
  agam_int __v313 = __v311 + __v312;
  agam_int __v315 = 1000000007;
  agam_int __v316 = __v313 % __v315;
  _local_checksum = __v316;
  agam_int __v317 = __v316;
  agam_int __v318 = _local_r;
  agam_int __v320 = 1;
  agam_int __v321 = __v318 + __v320;
  _local_r = __v321;
  agam_int __v322 = __v321;
  goto block_15;
block_17:
  agam_int __v323 = _local_checksum;
  return __v323;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_40:
  agam_int __v326 = 4096;
  agam_int __v327 = agam_bvh_traversal(__v326);
  printf("%lld", (long long)__v327);
  printf("\n");
  agam_int __v328 = 0;
  agam_int __v329 = 0;
  return (int)__v329;
}

