/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_zbuffer_rasterize(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_zbuffer_rasterize(agam_int __p0) {
  agam_int _local_triangles = __p0;
block_2:
  agam_int _local_width = 0;
  agam_int __v4 = 0;
  agam_int __v6 = 256;
  _local_width = __v6;
  agam_int __v7 = __v6;
  agam_int _local_height = 0;
  agam_int __v8 = 0;
  agam_int __v10 = 256;
  _local_height = __v10;
  agam_int __v11 = __v10;
  agam_int _local_checksum = 0;
  agam_int __v12 = 0;
  agam_int __v14 = 0;
  _local_checksum = __v14;
  agam_int __v15 = __v14;
  agam_int _local_t = 0;
  agam_int __v16 = 0;
  agam_int __v18 = 0;
  _local_t = __v18;
  agam_int __v19 = __v18;
  goto block_3;
block_3:
  agam_int __v20 = _local_t;
  agam_int __v21 = _local_triangles;
  agam_bool __v22 = __v20 < __v21;
  if (__v22) goto block_4; else goto block_5;
block_4:
  agam_int _local_x0 = 0;
  agam_int __v23 = 0;
  agam_int __v24 = _local_t;
  agam_int __v26 = 17;
  agam_int __v27 = __v24 * __v26;
  agam_int __v28 = _local_width;
  agam_int __v29 = __v27 % __v28;
  _local_x0 = __v29;
  agam_int __v30 = __v29;
  agam_int _local_y0 = 0;
  agam_int __v31 = 0;
  agam_int __v32 = _local_t;
  agam_int __v34 = 19;
  agam_int __v35 = __v32 * __v34;
  agam_int __v36 = _local_height;
  agam_int __v37 = __v35 % __v36;
  _local_y0 = __v37;
  agam_int __v38 = __v37;
  agam_int _local_z0 = 0;
  agam_int __v39 = 0;
  agam_int __v40 = _local_t;
  agam_int __v42 = 23;
  agam_int __v43 = __v40 * __v42;
  agam_int __v45 = 1000;
  agam_int __v46 = __v43 % __v45;
  _local_z0 = __v46;
  agam_int __v47 = __v46;
  agam_int _local_x1 = 0;
  agam_int __v48 = 0;
  agam_int __v49 = _local_x0;
  agam_int __v51 = 50;
  agam_int __v52 = __v49 + __v51;
  agam_int __v53 = _local_width;
  agam_int __v54 = __v52 % __v53;
  _local_x1 = __v54;
  agam_int __v55 = __v54;
  agam_int _local_y1 = 0;
  agam_int __v56 = 0;
  agam_int __v57 = _local_y0;
  agam_int __v59 = 20;
  agam_int __v60 = __v57 + __v59;
  agam_int __v61 = _local_height;
  agam_int __v62 = __v60 % __v61;
  _local_y1 = __v62;
  agam_int __v63 = __v62;
  agam_int _local_z1 = 0;
  agam_int __v64 = 0;
  agam_int __v65 = _local_t;
  agam_int __v67 = 29;
  agam_int __v68 = __v65 * __v67;
  agam_int __v70 = 1000;
  agam_int __v71 = __v68 % __v70;
  _local_z1 = __v71;
  agam_int __v72 = __v71;
  agam_int _local_x2 = 0;
  agam_int __v73 = 0;
  agam_int __v74 = _local_x0;
  agam_int __v76 = 20;
  agam_int __v77 = __v74 + __v76;
  agam_int __v78 = _local_width;
  agam_int __v79 = __v77 % __v78;
  _local_x2 = __v79;
  agam_int __v80 = __v79;
  agam_int _local_y2 = 0;
  agam_int __v81 = 0;
  agam_int __v82 = _local_y0;
  agam_int __v84 = 50;
  agam_int __v85 = __v82 + __v84;
  agam_int __v86 = _local_height;
  agam_int __v87 = __v85 % __v86;
  _local_y2 = __v87;
  agam_int __v88 = __v87;
  agam_int _local_z2 = 0;
  agam_int __v89 = 0;
  agam_int __v90 = _local_t;
  agam_int __v92 = 31;
  agam_int __v93 = __v90 * __v92;
  agam_int __v95 = 1000;
  agam_int __v96 = __v93 % __v95;
  _local_z2 = __v96;
  agam_int __v97 = __v96;
  agam_int _local_min_x = 0;
  agam_int __v98 = 0;
  agam_int __v99 = _local_x0;
  _local_min_x = __v99;
  agam_int __v100 = __v99;
  agam_int __v101 = _local_x1;
  agam_int __v102 = _local_min_x;
  agam_bool __v103 = __v101 < __v102;
  if (__v103) goto block_6; else goto block_8;
block_6:
  agam_int __v104 = _local_x1;
  _local_min_x = __v104;
  agam_int __v105 = __v104;
  goto block_8;
block_8:
  agam_int __v106 = _local_x2;
  agam_int __v107 = _local_min_x;
  agam_bool __v108 = __v106 < __v107;
  if (__v108) goto block_9; else goto block_11;
block_9:
  agam_int __v109 = _local_x2;
  _local_min_x = __v109;
  agam_int __v110 = __v109;
  goto block_11;
block_11:
  agam_int _local_max_x = 0;
  agam_int __v111 = 0;
  agam_int __v112 = _local_x0;
  _local_max_x = __v112;
  agam_int __v113 = __v112;
  agam_int __v114 = _local_x1;
  agam_int __v115 = _local_max_x;
  agam_bool __v116 = __v114 > __v115;
  if (__v116) goto block_12; else goto block_14;
block_12:
  agam_int __v117 = _local_x1;
  _local_max_x = __v117;
  agam_int __v118 = __v117;
  goto block_14;
block_14:
  agam_int __v119 = _local_x2;
  agam_int __v120 = _local_max_x;
  agam_bool __v121 = __v119 > __v120;
  if (__v121) goto block_15; else goto block_17;
block_15:
  agam_int __v122 = _local_x2;
  _local_max_x = __v122;
  agam_int __v123 = __v122;
  goto block_17;
block_17:
  agam_int _local_min_y = 0;
  agam_int __v124 = 0;
  agam_int __v125 = _local_y0;
  _local_min_y = __v125;
  agam_int __v126 = __v125;
  agam_int __v127 = _local_y1;
  agam_int __v128 = _local_min_y;
  agam_bool __v129 = __v127 < __v128;
  if (__v129) goto block_18; else goto block_20;
block_18:
  agam_int __v130 = _local_y1;
  _local_min_y = __v130;
  agam_int __v131 = __v130;
  goto block_20;
block_20:
  agam_int __v132 = _local_y2;
  agam_int __v133 = _local_min_y;
  agam_bool __v134 = __v132 < __v133;
  if (__v134) goto block_21; else goto block_23;
block_21:
  agam_int __v135 = _local_y2;
  _local_min_y = __v135;
  agam_int __v136 = __v135;
  goto block_23;
block_23:
  agam_int _local_max_y = 0;
  agam_int __v137 = 0;
  agam_int __v138 = _local_y0;
  _local_max_y = __v138;
  agam_int __v139 = __v138;
  agam_int __v140 = _local_y1;
  agam_int __v141 = _local_max_y;
  agam_bool __v142 = __v140 > __v141;
  if (__v142) goto block_24; else goto block_26;
block_24:
  agam_int __v143 = _local_y1;
  _local_max_y = __v143;
  agam_int __v144 = __v143;
  goto block_26;
block_26:
  agam_int __v145 = _local_y2;
  agam_int __v146 = _local_max_y;
  agam_bool __v147 = __v145 > __v146;
  if (__v147) goto block_27; else goto block_29;
block_27:
  agam_int __v148 = _local_y2;
  _local_max_y = __v148;
  agam_int __v149 = __v148;
  goto block_29;
block_29:
  agam_int _local_area = 0;
  agam_int __v150 = 0;
  agam_int __v151 = _local_x1;
  agam_int __v152 = _local_x0;
  agam_int __v153 = __v151 - __v152;
  agam_int __v154 = _local_y2;
  agam_int __v155 = _local_y0;
  agam_int __v156 = __v154 - __v155;
  agam_int __v157 = __v153 * __v156;
  agam_int __v158 = _local_x2;
  agam_int __v159 = _local_x0;
  agam_int __v160 = __v158 - __v159;
  agam_int __v161 = _local_y1;
  agam_int __v162 = _local_y0;
  agam_int __v163 = __v161 - __v162;
  agam_int __v164 = __v160 * __v163;
  agam_int __v165 = __v157 - __v164;
  _local_area = __v165;
  agam_int __v166 = __v165;
  agam_int __v167 = _local_area;
  agam_int __v169 = 0;
  agam_bool __v170 = __v167 == __v169;
  if (__v170) goto block_30; else goto block_32;
block_30:
  agam_int __v172 = 1;
  _local_area = __v172;
  agam_int __v173 = __v172;
  goto block_32;
block_32:
  agam_int __v174 = _local_area;
  agam_int __v176 = 0;
  agam_bool __v177 = __v174 < __v176;
  if (__v177) goto block_33; else goto block_35;
block_33:
  agam_int __v178 = _local_area;
  agam_int __v179 = -__v178;
  _local_area = __v179;
  agam_int __v180 = __v179;
  goto block_35;
block_35:
  agam_int _local_pixels_drawn = 0;
  agam_int __v181 = 0;
  agam_int __v183 = 0;
  _local_pixels_drawn = __v183;
  agam_int __v184 = __v183;
  agam_int _local_py = 0;
  agam_int __v185 = 0;
  agam_int __v186 = _local_min_y;
  _local_py = __v186;
  agam_int __v187 = __v186;
  goto block_36;
block_36:
  agam_int __v188 = _local_py;
  agam_int __v189 = _local_max_y;
  agam_bool __v190 = __v188 <= __v189;
  if (__v190) goto block_37; else goto block_38;
block_37:
  agam_int _local_px = 0;
  agam_int __v191 = 0;
  agam_int __v192 = _local_min_x;
  _local_px = __v192;
  agam_int __v193 = __v192;
  goto block_39;
block_39:
  agam_int __v194 = _local_px;
  agam_int __v195 = _local_max_x;
  agam_bool __v196 = __v194 <= __v195;
  if (__v196) goto block_40; else goto block_41;
block_40:
  agam_int _local_w0 = 0;
  agam_int __v197 = 0;
  agam_int __v198 = _local_x1;
  agam_int __v199 = _local_px;
  agam_int __v200 = __v198 - __v199;
  agam_int __v201 = _local_y2;
  agam_int __v202 = _local_py;
  agam_int __v203 = __v201 - __v202;
  agam_int __v204 = __v200 * __v203;
  agam_int __v205 = _local_x2;
  agam_int __v206 = _local_px;
  agam_int __v207 = __v205 - __v206;
  agam_int __v208 = _local_y1;
  agam_int __v209 = _local_py;
  agam_int __v210 = __v208 - __v209;
  agam_int __v211 = __v207 * __v210;
  agam_int __v212 = __v204 - __v211;
  _local_w0 = __v212;
  agam_int __v213 = __v212;
  agam_int _local_w1 = 0;
  agam_int __v214 = 0;
  agam_int __v215 = _local_x2;
  agam_int __v216 = _local_px;
  agam_int __v217 = __v215 - __v216;
  agam_int __v218 = _local_y0;
  agam_int __v219 = _local_py;
  agam_int __v220 = __v218 - __v219;
  agam_int __v221 = __v217 * __v220;
  agam_int __v222 = _local_x0;
  agam_int __v223 = _local_px;
  agam_int __v224 = __v222 - __v223;
  agam_int __v225 = _local_y2;
  agam_int __v226 = _local_py;
  agam_int __v227 = __v225 - __v226;
  agam_int __v228 = __v224 * __v227;
  agam_int __v229 = __v221 - __v228;
  _local_w1 = __v229;
  agam_int __v230 = __v229;
  agam_int _local_w2 = 0;
  agam_int __v231 = 0;
  agam_int __v232 = _local_x0;
  agam_int __v233 = _local_px;
  agam_int __v234 = __v232 - __v233;
  agam_int __v235 = _local_y1;
  agam_int __v236 = _local_py;
  agam_int __v237 = __v235 - __v236;
  agam_int __v238 = __v234 * __v237;
  agam_int __v239 = _local_x1;
  agam_int __v240 = _local_px;
  agam_int __v241 = __v239 - __v240;
  agam_int __v242 = _local_y0;
  agam_int __v243 = _local_py;
  agam_int __v244 = __v242 - __v243;
  agam_int __v245 = __v241 * __v244;
  agam_int __v246 = __v238 - __v245;
  _local_w2 = __v246;
  agam_int __v247 = __v246;
  agam_int _local_is_inside = 0;
  agam_int __v248 = 0;
  agam_int __v250 = 0;
  _local_is_inside = __v250;
  agam_int __v251 = __v250;
  agam_int __v252 = _local_w0;
  agam_int __v254 = 0;
  agam_bool __v255 = __v252 >= __v254;
  agam_int __v256 = _local_w1;
  agam_int __v258 = 0;
  agam_bool __v259 = __v256 >= __v258;
  agam_bool __v260 = __v255 && __v259;
  agam_int __v261 = _local_w2;
  agam_int __v263 = 0;
  agam_bool __v264 = __v261 >= __v263;
  agam_bool __v265 = __v260 && __v264;
  if (__v265) goto block_42; else goto block_44;
block_42:
  agam_int __v267 = 1;
  _local_is_inside = __v267;
  agam_int __v268 = __v267;
  goto block_44;
block_44:
  agam_int __v269 = _local_w0;
  agam_int __v271 = 0;
  agam_bool __v272 = __v269 <= __v271;
  agam_int __v273 = _local_w1;
  agam_int __v275 = 0;
  agam_bool __v276 = __v273 <= __v275;
  agam_bool __v277 = __v272 && __v276;
  agam_int __v278 = _local_w2;
  agam_int __v280 = 0;
  agam_bool __v281 = __v278 <= __v280;
  agam_bool __v282 = __v277 && __v281;
  if (__v282) goto block_45; else goto block_47;
block_45:
  agam_int __v284 = 1;
  _local_is_inside = __v284;
  agam_int __v285 = __v284;
  goto block_47;
block_47:
  agam_int __v286 = _local_is_inside;
  agam_int __v288 = 0;
  agam_bool __v289 = __v286 != __v288;
  if (__v289) goto block_48; else goto block_50;
block_48:
  agam_int _local_z = 0;
  agam_int __v290 = 0;
  agam_int __v291 = _local_w0;
  agam_int __v292 = _local_z0;
  agam_int __v293 = __v291 * __v292;
  agam_int __v294 = _local_w1;
  agam_int __v295 = _local_z1;
  agam_int __v296 = __v294 * __v295;
  agam_int __v297 = __v293 + __v296;
  agam_int __v298 = _local_w2;
  agam_int __v299 = _local_z2;
  agam_int __v300 = __v298 * __v299;
  agam_int __v301 = __v297 + __v300;
  agam_int __v302 = _local_area;
  agam_int __v303 = __v301 / __v302;
  _local_z = __v303;
  agam_int __v304 = __v303;
  agam_int __v305 = _local_pixels_drawn;
  agam_int __v307 = 1;
  agam_int __v308 = __v305 + __v307;
  _local_pixels_drawn = __v308;
  agam_int __v309 = __v308;
  agam_int __v310 = _local_checksum;
  agam_int __v312 = 31;
  agam_int __v313 = __v310 * __v312;
  agam_int __v314 = _local_z;
  agam_int __v315 = __v313 + __v314;
  agam_int __v316 = _local_px;
  agam_int __v317 = __v315 + __v316;
  agam_int __v318 = _local_py;
  agam_int __v319 = __v317 + __v318;
  agam_int __v321 = 1000000007;
  agam_int __v322 = __v319 % __v321;
  _local_checksum = __v322;
  agam_int __v323 = __v322;
  goto block_50;
block_50:
  agam_int __v324 = _local_px;
  agam_int __v326 = 1;
  agam_int __v327 = __v324 + __v326;
  _local_px = __v327;
  agam_int __v328 = __v327;
  goto block_39;
block_41:
  agam_int __v329 = _local_py;
  agam_int __v331 = 1;
  agam_int __v332 = __v329 + __v331;
  _local_py = __v332;
  agam_int __v333 = __v332;
  goto block_36;
block_38:
  agam_int __v334 = _local_checksum;
  agam_int __v336 = 37;
  agam_int __v337 = __v334 * __v336;
  agam_int __v338 = _local_pixels_drawn;
  agam_int __v339 = __v337 + __v338;
  agam_int __v341 = 1000000007;
  agam_int __v342 = __v339 % __v341;
  _local_checksum = __v342;
  agam_int __v343 = __v342;
  agam_int __v344 = _local_t;
  agam_int __v346 = 1;
  agam_int __v347 = __v344 + __v346;
  _local_t = __v347;
  agam_int __v348 = __v347;
  goto block_3;
block_5:
  agam_int __v349 = _local_checksum;
  return __v349;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_52:
  agam_int __v352 = 1024;
  agam_int __v353 = agam_zbuffer_rasterize(__v352);
  printf("%lld", (long long)__v353);
  printf("\n");
  agam_int __v354 = 0;
  agam_int __v355 = 0;
  return (int)__v355;
}

