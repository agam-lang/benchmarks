/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_max_val(agam_int __p0, agam_int __p1);
agam_int agam_min_val(agam_int __p0, agam_int __p1);
agam_int agam_non_zero_dir(agam_int __p0);
agam_int agam_bvh_traversal(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_max_val(agam_int __p0, agam_int __p1) {
  agam_int _local_a = __p0;
  agam_int _local_b = __p1;
block_2:
  agam_int __v5 = _local_a;
  agam_int __v6 = _local_b;
  agam_bool __v7 = __v5 > __v6;
  if (__v7) goto block_3; else goto block_5;
block_3:
  agam_int __v8 = _local_a;
  return __v8;
block_5:
  agam_int __v9 = _local_b;
  return __v9;
}

agam_int agam_min_val(agam_int __p0, agam_int __p1) {
  agam_int _local_a = __p0;
  agam_int _local_b = __p1;
block_8:
  agam_int __v13 = _local_a;
  agam_int __v14 = _local_b;
  agam_bool __v15 = __v13 < __v14;
  if (__v15) goto block_9; else goto block_11;
block_9:
  agam_int __v16 = _local_a;
  return __v16;
block_11:
  agam_int __v17 = _local_b;
  return __v17;
}

agam_int agam_non_zero_dir(agam_int __p0) {
  agam_int _local_value = __p0;
block_14:
  agam_int __v20 = _local_value;
  agam_int __v22 = 0;
  agam_bool __v23 = __v20 >= __v22;
  if (__v23) goto block_15; else goto block_17;
block_15:
  agam_int __v24 = _local_value;
  agam_int __v26 = 1;
  agam_int __v27 = __v24 + __v26;
  return __v27;
block_17:
  agam_int __v28 = _local_value;
  agam_int __v30 = 1;
  agam_int __v31 = __v28 - __v30;
  return __v31;
}

agam_int agam_bvh_traversal(agam_int __p0) {
  agam_int _local_rays = __p0;
block_20:
  agam_int _local_nodes = 0;
  agam_int __v34 = 0;
  agam_int __v36 = 256;
  _local_nodes = __v36;
  agam_int __v37 = __v36;
  agam_int _local_checksum = 0;
  agam_int __v38 = 0;
  agam_int __v40 = 0;
  _local_checksum = __v40;
  agam_int __v41 = __v40;
  agam_int _local_r = 0;
  agam_int __v42 = 0;
  agam_int __v44 = 0;
  _local_r = __v44;
  agam_int __v45 = __v44;
  goto block_21;
block_21:
  agam_int __v46 = _local_r;
  agam_int __v47 = _local_rays;
  agam_bool __v48 = __v46 < __v47;
  if (__v48) goto block_22; else goto block_23;
block_22:
  agam_int _local_ox = 0;
  agam_int __v49 = 0;
  agam_int __v50 = _local_r;
  agam_int __v52 = 17;
  agam_int __v53 = __v50 * __v52;
  agam_int __v55 = 1000;
  agam_int __v56 = __v53 % __v55;
  _local_ox = __v56;
  agam_int __v57 = __v56;
  agam_int _local_oy = 0;
  agam_int __v58 = 0;
  agam_int __v59 = _local_r;
  agam_int __v61 = 31;
  agam_int __v62 = __v59 * __v61;
  agam_int __v64 = 1000;
  agam_int __v65 = __v62 % __v64;
  _local_oy = __v65;
  agam_int __v66 = __v65;
  agam_int _local_dx = 0;
  agam_int __v72 = 0;
  agam_int __v73 = _local_r;
  agam_int __v75 = 13;
  agam_int __v76 = __v73 * __v75;
  agam_int __v78 = 20;
  agam_int __v79 = __v76 % __v78;
  agam_int __v81 = 10;
  agam_int __v82 = __v79 - __v81;
  _local_dx = __v82;
  agam_int __v83 = __v82;
  agam_int _local_dy = 0;
  agam_int __v84 = 0;
  agam_int __v85 = _local_r;
  agam_int __v87 = 19;
  agam_int __v88 = __v85 * __v87;
  agam_int __v90 = 20;
  agam_int __v91 = __v88 % __v90;
  agam_int __v93 = 10;
  agam_int __v94 = __v91 - __v93;
  _local_dy = __v94;
  agam_int __v95 = __v94;
  agam_int _local_dx_div = 0;
  agam_int __v100 = 0;
  agam_int __v101 = _local_dx;
  agam_int __v102 = agam_non_zero_dir(__v101);
  _local_dx_div = __v102;
  agam_int __v103 = __v102;
  agam_int _local_dy_div = 0;
  agam_int __v104 = 0;
  agam_int __v105 = _local_dy;
  agam_int __v106 = agam_non_zero_dir(__v105);
  _local_dy_div = __v106;
  agam_int __v107 = __v106;
  agam_int _local_stack_size = 0;
  agam_int __v108 = 0;
  agam_int __v110 = 0;
  _local_stack_size = __v110;
  agam_int __v111 = __v110;
  agam_int _local_current_node = 0;
  agam_int __v112 = 0;
  agam_int __v114 = 0;
  _local_current_node = __v114;
  agam_int __v115 = __v114;
  agam_int _local_hits = 0;
  agam_int __v116 = 0;
  agam_int __v118 = 0;
  _local_hits = __v118;
  agam_int __v119 = __v118;
  agam_int _local_steps = 0;
  agam_int __v120 = 0;
  agam_int __v122 = 0;
  _local_steps = __v122;
  agam_int __v123 = __v122;
  goto block_24;
block_24:
  agam_int __v124 = _local_steps;
  agam_int __v126 = 100;
  agam_bool __v127 = __v124 < __v126;
  if (__v127) goto block_25; else goto block_26;
block_25:
  agam_int _local_min_x = 0;
  agam_int __v128 = 0;
  agam_int __v129 = _local_current_node;
  agam_int __v131 = 7;
  agam_int __v132 = __v129 * __v131;
  agam_int __v134 = 1000;
  agam_int __v135 = __v132 % __v134;
  _local_min_x = __v135;
  agam_int __v136 = __v135;
  agam_int _local_max_x = 0;
  agam_int __v137 = 0;
  agam_int __v138 = _local_min_x;
  agam_int __v140 = 100;
  agam_int __v141 = __v138 + __v140;
  _local_max_x = __v141;
  agam_int __v142 = __v141;
  agam_int _local_min_y = 0;
  agam_int __v143 = 0;
  agam_int __v144 = _local_current_node;
  agam_int __v146 = 11;
  agam_int __v147 = __v144 * __v146;
  agam_int __v149 = 1000;
  agam_int __v150 = __v147 % __v149;
  _local_min_y = __v150;
  agam_int __v151 = __v150;
  agam_int _local_max_y = 0;
  agam_int __v152 = 0;
  agam_int __v153 = _local_min_y;
  agam_int __v155 = 100;
  agam_int __v156 = __v153 + __v155;
  _local_max_y = __v156;
  agam_int __v157 = __v156;
  agam_int _local_tmin_x_raw = 0;
  agam_int __v158 = 0;
  agam_int __v159 = _local_min_x;
  agam_int __v160 = _local_ox;
  agam_int __v161 = __v159 - __v160;
  agam_int __v163 = 100;
  agam_int __v164 = __v161 * __v163;
  agam_int __v165 = _local_dx_div;
  agam_int __v166 = __v164 / __v165;
  _local_tmin_x_raw = __v166;
  agam_int __v167 = __v166;
  agam_int _local_tmax_x_raw = 0;
  agam_int __v168 = 0;
  agam_int __v169 = _local_max_x;
  agam_int __v170 = _local_ox;
  agam_int __v171 = __v169 - __v170;
  agam_int __v173 = 100;
  agam_int __v174 = __v171 * __v173;
  agam_int __v175 = _local_dx_div;
  agam_int __v176 = __v174 / __v175;
  _local_tmax_x_raw = __v176;
  agam_int __v177 = __v176;
  agam_int _local_tmin_x = 0;
  agam_int __v178 = 0;
  agam_int __v179 = _local_tmin_x_raw;
  agam_int __v180 = _local_tmax_x_raw;
  agam_int __v181 = agam_min_val(__v179, __v180);
  _local_tmin_x = __v181;
  agam_int __v182 = __v181;
  agam_int _local_tmax_x = 0;
  agam_int __v183 = 0;
  agam_int __v184 = _local_tmin_x_raw;
  agam_int __v185 = _local_tmax_x_raw;
  agam_int __v186 = agam_max_val(__v184, __v185);
  _local_tmax_x = __v186;
  agam_int __v187 = __v186;
  agam_int _local_tmin_y_raw = 0;
  agam_int __v188 = 0;
  agam_int __v189 = _local_min_y;
  agam_int __v190 = _local_oy;
  agam_int __v191 = __v189 - __v190;
  agam_int __v193 = 100;
  agam_int __v194 = __v191 * __v193;
  agam_int __v195 = _local_dy_div;
  agam_int __v196 = __v194 / __v195;
  _local_tmin_y_raw = __v196;
  agam_int __v197 = __v196;
  agam_int _local_tmax_y_raw = 0;
  agam_int __v198 = 0;
  agam_int __v199 = _local_max_y;
  agam_int __v200 = _local_oy;
  agam_int __v201 = __v199 - __v200;
  agam_int __v203 = 100;
  agam_int __v204 = __v201 * __v203;
  agam_int __v205 = _local_dy_div;
  agam_int __v206 = __v204 / __v205;
  _local_tmax_y_raw = __v206;
  agam_int __v207 = __v206;
  agam_int _local_tmin_y = 0;
  agam_int __v208 = 0;
  agam_int __v209 = _local_tmin_y_raw;
  agam_int __v210 = _local_tmax_y_raw;
  agam_int __v211 = agam_min_val(__v209, __v210);
  _local_tmin_y = __v211;
  agam_int __v212 = __v211;
  agam_int _local_tmax_y = 0;
  agam_int __v213 = 0;
  agam_int __v214 = _local_tmin_y_raw;
  agam_int __v215 = _local_tmax_y_raw;
  agam_int __v216 = agam_max_val(__v214, __v215);
  _local_tmax_y = __v216;
  agam_int __v217 = __v216;
  agam_int _local_tmin = 0;
  agam_int __v218 = 0;
  agam_int __v219 = _local_tmin_x;
  _local_tmin = __v219;
  agam_int __v220 = __v219;
  agam_int __v221 = _local_tmin_y;
  agam_int __v222 = _local_tmin;
  agam_bool __v223 = __v221 > __v222;
  if (__v223) goto block_27; else goto block_29;
block_27:
  agam_int __v224 = _local_tmin_y;
  _local_tmin = __v224;
  agam_int __v225 = __v224;
  goto block_29;
block_29:
  agam_int _local_tmax = 0;
  agam_int __v226 = 0;
  agam_int __v227 = _local_tmax_x;
  _local_tmax = __v227;
  agam_int __v228 = __v227;
  agam_int __v229 = _local_tmax_y;
  agam_int __v230 = _local_tmax;
  agam_bool __v231 = __v229 < __v230;
  if (__v231) goto block_30; else goto block_32;
block_30:
  agam_int __v232 = _local_tmax_y;
  _local_tmax = __v232;
  agam_int __v233 = __v232;
  goto block_32;
block_32:
  agam_int __v234 = _local_tmax;
  agam_int __v235 = _local_tmin;
  agam_bool __v236 = __v234 >= __v235;
  agam_int __v237 = _local_tmax;
  agam_int __v239 = 0;
  agam_bool __v240 = __v237 > __v239;
  agam_bool __v241 = __v236 && __v240;
  if (__v241) goto block_33; else goto block_34;
block_33:
  agam_int _local_is_leaf = 0;
  agam_int __v242 = 0;
  agam_int __v243 = _local_current_node;
  agam_int __v245 = 3;
  agam_int __v246 = __v243 % __v245;
  agam_int __v248 = 0;
  agam_bool __v249 = __v246 == __v248;
  _local_is_leaf = __v249;
  agam_int __v250 = __v249;
  agam_int __v251 = _local_is_leaf;
  agam_int __v253 = 0;
  agam_bool __v254 = __v251 != __v253;
  if (__v254) goto block_36; else goto block_37;
block_36:
  agam_int __v255 = _local_hits;
  agam_int __v257 = 1;
  agam_int __v258 = __v255 + __v257;
  _local_hits = __v258;
  agam_int __v259 = __v258;
  agam_int __v260 = _local_stack_size;
  agam_int __v262 = 1;
  agam_int __v263 = __v260 - __v262;
  _local_stack_size = __v263;
  agam_int __v264 = __v263;
  agam_int __v265 = _local_stack_size;
  agam_int __v267 = 0;
  agam_bool __v268 = __v265 < __v267;
  if (__v268) goto block_39; else goto block_41;
block_39:
  agam_int __v270 = 999;
  _local_steps = __v270;
  agam_int __v271 = __v270;
  goto block_41;
block_41:
  agam_int __v272 = _local_current_node;
  agam_int __v274 = 2;
  agam_int __v275 = __v272 * __v274;
  agam_int __v276 = _local_nodes;
  agam_int __v277 = __v275 % __v276;
  _local_current_node = __v277;
  agam_int __v278 = __v277;
  goto block_38;
block_37:
  agam_int __v279 = _local_stack_size;
  agam_int __v281 = 1;
  agam_int __v282 = __v279 + __v281;
  _local_stack_size = __v282;
  agam_int __v283 = __v282;
  agam_int __v284 = _local_current_node;
  agam_int __v286 = 2;
  agam_int __v287 = __v284 * __v286;
  agam_int __v289 = 1;
  agam_int __v290 = __v287 + __v289;
  agam_int __v291 = _local_nodes;
  agam_int __v292 = __v290 % __v291;
  _local_current_node = __v292;
  agam_int __v293 = __v292;
  goto block_38;
block_38:
  goto block_35;
block_34:
  agam_int __v294 = _local_stack_size;
  agam_int __v296 = 1;
  agam_int __v297 = __v294 - __v296;
  _local_stack_size = __v297;
  agam_int __v298 = __v297;
  agam_int __v299 = _local_stack_size;
  agam_int __v301 = 0;
  agam_bool __v302 = __v299 < __v301;
  if (__v302) goto block_42; else goto block_44;
block_42:
  agam_int __v304 = 999;
  _local_steps = __v304;
  agam_int __v305 = __v304;
  goto block_44;
block_44:
  agam_int __v306 = _local_current_node;
  agam_int __v308 = 1;
  agam_int __v309 = __v306 + __v308;
  agam_int __v310 = _local_nodes;
  agam_int __v311 = __v309 % __v310;
  _local_current_node = __v311;
  agam_int __v312 = __v311;
  goto block_35;
block_35:
  agam_int __v313 = _local_steps;
  agam_int __v315 = 1;
  agam_int __v316 = __v313 + __v315;
  _local_steps = __v316;
  agam_int __v317 = __v316;
  goto block_24;
block_26:
  agam_int __v318 = _local_checksum;
  agam_int __v320 = 31;
  agam_int __v321 = __v318 * __v320;
  agam_int __v322 = _local_hits;
  agam_int __v323 = __v321 + __v322;
  agam_int __v325 = 1000000007;
  agam_int __v326 = __v323 % __v325;
  _local_checksum = __v326;
  agam_int __v327 = __v326;
  agam_int __v328 = _local_r;
  agam_int __v330 = 1;
  agam_int __v331 = __v328 + __v330;
  _local_r = __v331;
  agam_int __v332 = __v331;
  goto block_21;
block_23:
  agam_int __v333 = _local_checksum;
  return __v333;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_46:
  agam_int __v336 = 4096;
  agam_int __v337 = agam_bvh_traversal(__v336);
  printf("%lld", (long long)__v337);
  printf("\n");
  agam_int __v338 = 0;
  agam_int __v339 = 0;
  return (int)__v339;
}

