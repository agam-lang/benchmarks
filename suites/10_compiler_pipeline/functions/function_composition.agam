@lang.advance
// Test: composing function results.
fn inc(x: i64) -> i64 { return x + 1; }
fn square(x: i64) -> i64 { return x * x; }
fn main() -> i32 { print_int(square(inc(inc(inc(4))))); return 0; }
