@lang.advance
// Test: if/else, while loop parsing.
fn main() -> i32 {
    let x: i64 = 0;
    let i: i64 = 0;
    while i < 10 {
        if i % 2 == 0 { x = x + i; } else { x = x - 1; }
        i = i + 1;
    }
    print_int(x);
    return 0;
}
