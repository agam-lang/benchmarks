@lang.advance
// Test: function definitions with multiple params and return types.
fn add(a: i64, b: i64) -> i64 { return a + b; }
fn square(x: i64) -> i64 { return x * x; }
fn main() -> i32 { print_int(add(square(3), square(4))); return 0; }
