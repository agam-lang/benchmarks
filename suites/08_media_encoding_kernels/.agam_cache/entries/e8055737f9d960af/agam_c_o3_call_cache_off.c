/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_audio_lpc_checksum(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_audio_lpc_checksum(agam_int __p0) {
  agam_int _local_frames = __p0;
block_2:
  agam_int _local_order = 0;
  agam_int __v4 = 0;
  agam_int __v6 = 10;
  _local_order = __v6;
  agam_int __v7 = __v6;
  agam_int _local_frame_size = 0;
  agam_int __v8 = 0;
  agam_int __v10 = 256;
  _local_frame_size = __v10;
  agam_int __v11 = __v10;
  agam_int _local_checksum = 0;
  agam_int __v12 = 0;
  agam_int __v14 = 0;
  _local_checksum = __v14;
  agam_int __v15 = __v14;
  agam_int _local_f = 0;
  agam_int __v16 = 0;
  agam_int __v18 = 0;
  _local_f = __v18;
  agam_int __v19 = __v18;
  goto block_3;
block_3:
  agam_int __v20 = _local_f;
  agam_int __v21 = _local_frames;
  agam_bool __v22 = __v20 < __v21;
  if (__v22) goto block_4; else goto block_5;
block_4:
  agam_int _local_max_r = 0;
  agam_int __v23 = 0;
  agam_int __v25 = 0;
  _local_max_r = __v25;
  agam_int __v26 = __v25;
  agam_int _local_lag = 0;
  agam_int __v27 = 0;
  agam_int __v29 = 0;
  _local_lag = __v29;
  agam_int __v30 = __v29;
  goto block_6;
block_6:
  agam_int __v31 = _local_lag;
  agam_int __v32 = _local_order;
  agam_bool __v33 = __v31 <= __v32;
  if (__v33) goto block_7; else goto block_8;
block_7:
  agam_int _local_r = 0;
  agam_int __v34 = 0;
  agam_int __v36 = 0;
  _local_r = __v36;
  agam_int __v37 = __v36;
  agam_int _local_i = 0;
  agam_int __v38 = 0;
  agam_int __v40 = 0;
  _local_i = __v40;
  agam_int __v41 = __v40;
  goto block_9;
block_9:
  agam_int __v42 = _local_i;
  agam_int __v43 = _local_frame_size;
  agam_int __v44 = _local_lag;
  agam_int __v45 = __v43 - __v44;
  agam_bool __v46 = __v42 < __v45;
  if (__v46) goto block_10; else goto block_11;
block_10:
  agam_int _local_s1 = 0;
  agam_int __v47 = 0;
  agam_int __v48 = _local_f;
  agam_int __v49 = _local_frame_size;
  agam_int __v50 = __v48 * __v49;
  agam_int __v51 = _local_i;
  agam_int __v52 = __v50 + __v51;
  agam_int __v54 = 17;
  agam_int __v55 = __v52 * __v54;
  agam_int __v57 = 13;
  agam_int __v58 = __v55 + __v57;
  agam_int __v60 = 65536;
  agam_int __v61 = __v58 % __v60;
  agam_int __v63 = 32768;
  agam_int __v64 = __v61 - __v63;
  _local_s1 = __v64;
  agam_int __v65 = __v64;
  agam_int _local_s2 = 0;
  agam_int __v66 = 0;
  agam_int __v67 = _local_f;
  agam_int __v68 = _local_frame_size;
  agam_int __v69 = __v67 * __v68;
  agam_int __v70 = _local_i;
  agam_int __v71 = __v69 + __v70;
  agam_int __v72 = _local_lag;
  agam_int __v73 = __v71 + __v72;
  agam_int __v75 = 17;
  agam_int __v76 = __v73 * __v75;
  agam_int __v78 = 13;
  agam_int __v79 = __v76 + __v78;
  agam_int __v81 = 65536;
  agam_int __v82 = __v79 % __v81;
  agam_int __v84 = 32768;
  agam_int __v85 = __v82 - __v84;
  _local_s2 = __v85;
  agam_int __v86 = __v85;
  agam_int __v87 = _local_r;
  agam_int __v88 = _local_s1;
  agam_int __v89 = _local_s2;
  agam_int __v90 = __v88 * __v89;
  agam_int __v92 = 32768;
  agam_int __v93 = __v90 / __v92;
  agam_int __v94 = __v87 + __v93;
  _local_r = __v94;
  agam_int __v95 = __v94;
  agam_int __v96 = _local_i;
  agam_int __v98 = 1;
  agam_int __v99 = __v96 + __v98;
  _local_i = __v99;
  agam_int __v100 = __v99;
  goto block_9;
block_11:
  agam_int __v101 = _local_r;
  agam_int __v103 = 0;
  agam_bool __v104 = __v101 < __v103;
  if (__v104) goto block_12; else goto block_14;
block_12:
  agam_int __v105 = _local_r;
  agam_int __v106 = -__v105;
  _local_r = __v106;
  agam_int __v107 = __v106;
  goto block_14;
block_14:
  agam_int __v108 = _local_r;
  agam_int __v109 = _local_max_r;
  agam_bool __v110 = __v108 > __v109;
  if (__v110) goto block_15; else goto block_17;
block_15:
  agam_int __v111 = _local_r;
  _local_max_r = __v111;
  agam_int __v112 = __v111;
  goto block_17;
block_17:
  agam_int __v113 = _local_checksum;
  agam_int __v115 = 31;
  agam_int __v116 = __v113 * __v115;
  agam_int __v117 = _local_r;
  agam_int __v118 = __v116 + __v117;
  agam_int __v120 = 1000000007;
  agam_int __v121 = __v118 % __v120;
  _local_checksum = __v121;
  agam_int __v122 = __v121;
  agam_int __v123 = _local_lag;
  agam_int __v125 = 1;
  agam_int __v126 = __v123 + __v125;
  _local_lag = __v126;
  agam_int __v127 = __v126;
  goto block_6;
block_8:
  agam_int _local_residual = 0;
  agam_int __v128 = 0;
  agam_int __v130 = 0;
  _local_residual = __v130;
  agam_int __v131 = __v130;
  agam_int _local_i_r = 0;
  agam_int __v132 = 0;
  agam_int __v133 = _local_order;
  _local_i_r = __v133;
  agam_int __v134 = __v133;
  goto block_18;
block_18:
  agam_int __v135 = _local_i_r;
  agam_int __v136 = _local_frame_size;
  agam_bool __v137 = __v135 < __v136;
  if (__v137) goto block_19; else goto block_20;
block_19:
  agam_int _local_pred = 0;
  agam_int __v138 = 0;
  agam_int __v140 = 0;
  _local_pred = __v140;
  agam_int __v141 = __v140;
  agam_int _local_j = 0;
  agam_int __v142 = 0;
  agam_int __v144 = 1;
  _local_j = __v144;
  agam_int __v145 = __v144;
  goto block_21;
block_21:
  agam_int __v146 = _local_j;
  agam_int __v147 = _local_order;
  agam_bool __v148 = __v146 <= __v147;
  if (__v148) goto block_22; else goto block_23;
block_22:
  agam_int _local_s = 0;
  agam_int __v149 = 0;
  agam_int __v150 = _local_f;
  agam_int __v151 = _local_frame_size;
  agam_int __v152 = __v150 * __v151;
  agam_int __v153 = _local_i_r;
  agam_int __v154 = __v152 + __v153;
  agam_int __v155 = _local_j;
  agam_int __v156 = __v154 - __v155;
  agam_int __v158 = 17;
  agam_int __v159 = __v156 * __v158;
  agam_int __v161 = 13;
  agam_int __v162 = __v159 + __v161;
  agam_int __v164 = 65536;
  agam_int __v165 = __v162 % __v164;
  agam_int __v167 = 32768;
  agam_int __v168 = __v165 - __v167;
  _local_s = __v168;
  agam_int __v169 = __v168;
  agam_int _local_coef = 0;
  agam_int __v170 = 0;
  agam_int __v171 = _local_j;
  agam_int __v173 = 17;
  agam_int __v174 = __v171 * __v173;
  agam_int __v176 = 100;
  agam_int __v177 = __v174 % __v176;
  _local_coef = __v177;
  agam_int __v178 = __v177;
  agam_int __v179 = _local_pred;
  agam_int __v180 = _local_s;
  agam_int __v181 = _local_coef;
  agam_int __v182 = __v180 * __v181;
  agam_int __v184 = 100;
  agam_int __v185 = __v182 / __v184;
  agam_int __v186 = __v179 + __v185;
  _local_pred = __v186;
  agam_int __v187 = __v186;
  agam_int __v188 = _local_j;
  agam_int __v190 = 1;
  agam_int __v191 = __v188 + __v190;
  _local_j = __v191;
  agam_int __v192 = __v191;
  goto block_21;
block_23:
  agam_int _local_actual = 0;
  agam_int __v193 = 0;
  agam_int __v194 = _local_f;
  agam_int __v195 = _local_frame_size;
  agam_int __v196 = __v194 * __v195;
  agam_int __v197 = _local_i_r;
  agam_int __v198 = __v196 + __v197;
  agam_int __v200 = 17;
  agam_int __v201 = __v198 * __v200;
  agam_int __v203 = 13;
  agam_int __v204 = __v201 + __v203;
  agam_int __v206 = 65536;
  agam_int __v207 = __v204 % __v206;
  agam_int __v209 = 32768;
  agam_int __v210 = __v207 - __v209;
  _local_actual = __v210;
  agam_int __v211 = __v210;
  agam_int _local_diff = 0;
  agam_int __v212 = 0;
  agam_int __v213 = _local_actual;
  agam_int __v214 = _local_pred;
  agam_int __v215 = __v213 - __v214;
  _local_diff = __v215;
  agam_int __v216 = __v215;
  agam_int __v217 = _local_diff;
  agam_int __v219 = 0;
  agam_bool __v220 = __v217 < __v219;
  if (__v220) goto block_24; else goto block_26;
block_24:
  agam_int __v221 = _local_diff;
  agam_int __v222 = -__v221;
  _local_diff = __v222;
  agam_int __v223 = __v222;
  goto block_26;
block_26:
  agam_int __v224 = _local_residual;
  agam_int __v225 = _local_diff;
  agam_int __v226 = __v224 + __v225;
  _local_residual = __v226;
  agam_int __v227 = __v226;
  agam_int __v228 = _local_i_r;
  agam_int __v230 = 1;
  agam_int __v231 = __v228 + __v230;
  _local_i_r = __v231;
  agam_int __v232 = __v231;
  goto block_18;
block_20:
  agam_int __v233 = _local_checksum;
  agam_int __v235 = 37;
  agam_int __v236 = __v233 * __v235;
  agam_int __v237 = _local_residual;
  agam_int __v238 = __v236 + __v237;
  agam_int __v239 = _local_max_r;
  agam_int __v240 = __v238 + __v239;
  agam_int __v242 = 1000000007;
  agam_int __v243 = __v240 % __v242;
  _local_checksum = __v243;
  agam_int __v244 = __v243;
  agam_int __v245 = _local_f;
  agam_int __v247 = 1;
  agam_int __v248 = __v245 + __v247;
  _local_f = __v248;
  agam_int __v249 = __v248;
  goto block_3;
block_5:
  agam_int __v250 = _local_checksum;
  return __v250;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_28:
  agam_int __v253 = 32;
  agam_int __v254 = agam_audio_lpc_checksum(__v253);
  printf("%lld", (long long)__v254);
  printf("\n");
  agam_int __v255 = 0;
  agam_int __v256 = 0;
  return (int)__v256;
}

