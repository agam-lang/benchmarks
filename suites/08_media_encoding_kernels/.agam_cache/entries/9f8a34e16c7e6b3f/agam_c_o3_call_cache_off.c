/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_clamp(agam_int __p0);
agam_int agam_pixel_filter_checksum(agam_int __p0, agam_int __p1);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_clamp(agam_int __p0) {
  agam_int _local_val = __p0;
block_2:
  agam_int __v4 = _local_val;
  agam_int __v6 = 0;
  agam_bool __v7 = __v4 < __v6;
  if (__v7) goto block_3; else goto block_5;
block_3:
  agam_int __v9 = 0;
  return __v9;
block_5:
  agam_int __v10 = _local_val;
  agam_int __v12 = 255;
  agam_bool __v13 = __v10 > __v12;
  if (__v13) goto block_7; else goto block_9;
block_7:
  agam_int __v15 = 255;
  return __v15;
block_9:
  agam_int __v16 = _local_val;
  return __v16;
}

agam_int agam_pixel_filter_checksum(agam_int __p0, agam_int __p1) {
  agam_int _local_width = __p0;
  agam_int _local_height = __p1;
block_12:
  agam_int _local_checksum = 0;
  agam_int __v20 = 0;
  agam_int __v22 = 0;
  _local_checksum = __v22;
  agam_int __v23 = __v22;
  agam_int _local_y = 0;
  agam_int __v24 = 0;
  agam_int __v26 = 1;
  _local_y = __v26;
  agam_int __v27 = __v26;
  goto block_13;
block_13:
  agam_int __v28 = _local_y;
  agam_int __v29 = _local_height;
  agam_int __v31 = 1;
  agam_int __v32 = __v29 - __v31;
  agam_bool __v33 = __v28 < __v32;
  if (__v33) goto block_14; else goto block_15;
block_14:
  agam_int _local_x = 0;
  agam_int __v34 = 0;
  agam_int __v36 = 1;
  _local_x = __v36;
  agam_int __v37 = __v36;
  goto block_16;
block_16:
  agam_int __v38 = _local_x;
  agam_int __v39 = _local_width;
  agam_int __v41 = 1;
  agam_int __v42 = __v39 - __v41;
  agam_bool __v43 = __v38 < __v42;
  if (__v43) goto block_17; else goto block_18;
block_17:
  agam_int _local_p00 = 0;
  agam_int __v44 = 0;
  agam_int __v45 = _local_y;
  agam_int __v47 = 1;
  agam_int __v48 = __v45 - __v47;
  agam_int __v49 = _local_width;
  agam_int __v50 = __v48 * __v49;
  agam_int __v51 = _local_x;
  agam_int __v53 = 1;
  agam_int __v54 = __v51 - __v53;
  agam_int __v55 = __v50 + __v54;
  agam_int __v57 = 17;
  agam_int __v58 = __v55 * __v57;
  agam_int __v60 = 13;
  agam_int __v61 = __v58 + __v60;
  agam_int __v63 = 256;
  agam_int __v64 = __v61 % __v63;
  _local_p00 = __v64;
  agam_int __v65 = __v64;
  agam_int _local_p01 = 0;
  agam_int __v66 = 0;
  agam_int __v67 = _local_y;
  agam_int __v69 = 1;
  agam_int __v70 = __v67 - __v69;
  agam_int __v71 = _local_width;
  agam_int __v72 = __v70 * __v71;
  agam_int __v73 = _local_x;
  agam_int __v74 = __v72 + __v73;
  agam_int __v76 = 17;
  agam_int __v77 = __v74 * __v76;
  agam_int __v79 = 13;
  agam_int __v80 = __v77 + __v79;
  agam_int __v82 = 256;
  agam_int __v83 = __v80 % __v82;
  _local_p01 = __v83;
  agam_int __v84 = __v83;
  agam_int _local_p02 = 0;
  agam_int __v85 = 0;
  agam_int __v86 = _local_y;
  agam_int __v88 = 1;
  agam_int __v89 = __v86 - __v88;
  agam_int __v90 = _local_width;
  agam_int __v91 = __v89 * __v90;
  agam_int __v92 = _local_x;
  agam_int __v94 = 1;
  agam_int __v95 = __v92 + __v94;
  agam_int __v96 = __v91 + __v95;
  agam_int __v98 = 17;
  agam_int __v99 = __v96 * __v98;
  agam_int __v101 = 13;
  agam_int __v102 = __v99 + __v101;
  agam_int __v104 = 256;
  agam_int __v105 = __v102 % __v104;
  _local_p02 = __v105;
  agam_int __v106 = __v105;
  agam_int _local_p10 = 0;
  agam_int __v107 = 0;
  agam_int __v108 = _local_y;
  agam_int __v109 = _local_width;
  agam_int __v110 = __v108 * __v109;
  agam_int __v111 = _local_x;
  agam_int __v113 = 1;
  agam_int __v114 = __v111 - __v113;
  agam_int __v115 = __v110 + __v114;
  agam_int __v117 = 17;
  agam_int __v118 = __v115 * __v117;
  agam_int __v120 = 13;
  agam_int __v121 = __v118 + __v120;
  agam_int __v123 = 256;
  agam_int __v124 = __v121 % __v123;
  _local_p10 = __v124;
  agam_int __v125 = __v124;
  agam_int _local_p11 = 0;
  agam_int __v126 = 0;
  agam_int __v127 = _local_y;
  agam_int __v128 = _local_width;
  agam_int __v129 = __v127 * __v128;
  agam_int __v130 = _local_x;
  agam_int __v131 = __v129 + __v130;
  agam_int __v133 = 17;
  agam_int __v134 = __v131 * __v133;
  agam_int __v136 = 13;
  agam_int __v137 = __v134 + __v136;
  agam_int __v139 = 256;
  agam_int __v140 = __v137 % __v139;
  _local_p11 = __v140;
  agam_int __v141 = __v140;
  agam_int _local_p12 = 0;
  agam_int __v142 = 0;
  agam_int __v143 = _local_y;
  agam_int __v144 = _local_width;
  agam_int __v145 = __v143 * __v144;
  agam_int __v146 = _local_x;
  agam_int __v148 = 1;
  agam_int __v149 = __v146 + __v148;
  agam_int __v150 = __v145 + __v149;
  agam_int __v152 = 17;
  agam_int __v153 = __v150 * __v152;
  agam_int __v155 = 13;
  agam_int __v156 = __v153 + __v155;
  agam_int __v158 = 256;
  agam_int __v159 = __v156 % __v158;
  _local_p12 = __v159;
  agam_int __v160 = __v159;
  agam_int _local_p20 = 0;
  agam_int __v161 = 0;
  agam_int __v162 = _local_y;
  agam_int __v164 = 1;
  agam_int __v165 = __v162 + __v164;
  agam_int __v166 = _local_width;
  agam_int __v167 = __v165 * __v166;
  agam_int __v168 = _local_x;
  agam_int __v170 = 1;
  agam_int __v171 = __v168 - __v170;
  agam_int __v172 = __v167 + __v171;
  agam_int __v174 = 17;
  agam_int __v175 = __v172 * __v174;
  agam_int __v177 = 13;
  agam_int __v178 = __v175 + __v177;
  agam_int __v180 = 256;
  agam_int __v181 = __v178 % __v180;
  _local_p20 = __v181;
  agam_int __v182 = __v181;
  agam_int _local_p21 = 0;
  agam_int __v183 = 0;
  agam_int __v184 = _local_y;
  agam_int __v186 = 1;
  agam_int __v187 = __v184 + __v186;
  agam_int __v188 = _local_width;
  agam_int __v189 = __v187 * __v188;
  agam_int __v190 = _local_x;
  agam_int __v191 = __v189 + __v190;
  agam_int __v193 = 17;
  agam_int __v194 = __v191 * __v193;
  agam_int __v196 = 13;
  agam_int __v197 = __v194 + __v196;
  agam_int __v199 = 256;
  agam_int __v200 = __v197 % __v199;
  _local_p21 = __v200;
  agam_int __v201 = __v200;
  agam_int _local_p22 = 0;
  agam_int __v202 = 0;
  agam_int __v203 = _local_y;
  agam_int __v205 = 1;
  agam_int __v206 = __v203 + __v205;
  agam_int __v207 = _local_width;
  agam_int __v208 = __v206 * __v207;
  agam_int __v209 = _local_x;
  agam_int __v211 = 1;
  agam_int __v212 = __v209 + __v211;
  agam_int __v213 = __v208 + __v212;
  agam_int __v215 = 17;
  agam_int __v216 = __v213 * __v215;
  agam_int __v218 = 13;
  agam_int __v219 = __v216 + __v218;
  agam_int __v221 = 256;
  agam_int __v222 = __v219 % __v221;
  _local_p22 = __v222;
  agam_int __v223 = __v222;
  agam_int _local_blur = 0;
  agam_int __v224 = 0;
  agam_int __v225 = _local_p00;
  agam_int __v227 = 2;
  agam_int __v228 = _local_p01;
  agam_int __v229 = __v227 * __v228;
  agam_int __v230 = __v225 + __v229;
  agam_int __v231 = _local_p02;
  agam_int __v232 = __v230 + __v231;
  agam_int __v234 = 2;
  agam_int __v235 = _local_p10;
  agam_int __v236 = __v234 * __v235;
  agam_int __v237 = __v232 + __v236;
  agam_int __v239 = 4;
  agam_int __v240 = _local_p11;
  agam_int __v241 = __v239 * __v240;
  agam_int __v242 = __v237 + __v241;
  agam_int __v244 = 2;
  agam_int __v245 = _local_p12;
  agam_int __v246 = __v244 * __v245;
  agam_int __v247 = __v242 + __v246;
  agam_int __v248 = _local_p20;
  agam_int __v249 = __v247 + __v248;
  agam_int __v251 = 2;
  agam_int __v252 = _local_p21;
  agam_int __v253 = __v251 * __v252;
  agam_int __v254 = __v249 + __v253;
  agam_int __v255 = _local_p22;
  agam_int __v256 = __v254 + __v255;
  agam_int __v258 = 16;
  agam_int __v259 = __v256 / __v258;
  _local_blur = __v259;
  agam_int __v260 = __v259;
  agam_int _local_sobel_x = 0;
  agam_int __v261 = 0;
  agam_int __v262 = _local_p00;
  agam_int __v263 = -__v262;
  agam_int __v264 = _local_p02;
  agam_int __v265 = __v263 + __v264;
  agam_int __v267 = 2;
  agam_int __v268 = _local_p10;
  agam_int __v269 = __v267 * __v268;
  agam_int __v270 = __v265 - __v269;
  agam_int __v272 = 2;
  agam_int __v273 = _local_p12;
  agam_int __v274 = __v272 * __v273;
  agam_int __v275 = __v270 + __v274;
  agam_int __v276 = _local_p20;
  agam_int __v277 = __v275 - __v276;
  agam_int __v278 = _local_p22;
  agam_int __v279 = __v277 + __v278;
  _local_sobel_x = __v279;
  agam_int __v280 = __v279;
  agam_int _local_sobel_y = 0;
  agam_int __v281 = 0;
  agam_int __v282 = _local_p00;
  agam_int __v283 = -__v282;
  agam_int __v285 = 2;
  agam_int __v286 = _local_p01;
  agam_int __v287 = __v285 * __v286;
  agam_int __v288 = __v283 - __v287;
  agam_int __v289 = _local_p02;
  agam_int __v290 = __v288 - __v289;
  agam_int __v291 = _local_p20;
  agam_int __v292 = __v290 + __v291;
  agam_int __v294 = 2;
  agam_int __v295 = _local_p21;
  agam_int __v296 = __v294 * __v295;
  agam_int __v297 = __v292 + __v296;
  agam_int __v298 = _local_p22;
  agam_int __v299 = __v297 + __v298;
  _local_sobel_y = __v299;
  agam_int __v300 = __v299;
  agam_int _local_sobel = 0;
  agam_int __v301 = 0;
  agam_int __v302 = _local_sobel_x;
  agam_int __v303 = _local_sobel_x;
  agam_int __v304 = __v302 * __v303;
  agam_int __v305 = _local_sobel_y;
  agam_int __v306 = _local_sobel_y;
  agam_int __v307 = __v305 * __v306;
  agam_int __v308 = __v304 + __v307;
  _local_sobel = __v308;
  agam_int __v309 = __v308;
  agam_int _local_edge = 0;
  agam_int __v310 = 0;
  agam_int __v311 = _local_sobel;
  agam_int __v313 = 100;
  agam_int __v314 = __v311 / __v313;
  _local_edge = __v314;
  agam_int __v315 = __v314;
  agam_int _local_out = 0;
  agam_int __v316 = 0;
  agam_int __v317 = _local_blur;
  agam_int __v318 = _local_edge;
  agam_int __v319 = __v317 + __v318;
  agam_int __v320 = agam_clamp(__v319);
  _local_out = __v320;
  agam_int __v321 = __v320;
  agam_int __v322 = _local_checksum;
  agam_int __v324 = 31;
  agam_int __v325 = __v322 * __v324;
  agam_int __v326 = _local_out;
  agam_int __v327 = __v325 + __v326;
  agam_int __v329 = 1000000007;
  agam_int __v330 = __v327 % __v329;
  _local_checksum = __v330;
  agam_int __v331 = __v330;
  agam_int __v332 = _local_x;
  agam_int __v334 = 1;
  agam_int __v335 = __v332 + __v334;
  _local_x = __v335;
  agam_int __v336 = __v335;
  goto block_16;
block_18:
  agam_int __v337 = _local_y;
  agam_int __v339 = 1;
  agam_int __v340 = __v337 + __v339;
  _local_y = __v340;
  agam_int __v341 = __v340;
  goto block_13;
block_15:
  agam_int __v342 = _local_checksum;
  return __v342;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_20:
  agam_int __v345 = 256;
  agam_int __v347 = 256;
  agam_int __v348 = agam_pixel_filter_checksum(__v345, __v347);
  printf("%lld", (long long)__v348);
  printf("\n");
  agam_int __v349 = 0;
  agam_int __v350 = 0;
  return (int)__v350;
}

