@lang.advance
fn compute(n: i64) -> i64 {
    let total: i64 = 0;
    let i: i64 = 0;
    while i < n {
        total = total + ((i * 31) + 7) % 4099;
        i = i + 1;
    }
    return total;
}
fn main() -> i32 { print_int(compute(10000)); return 0; }
