/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_abs_val(agam_int __p0);
agam_int agam_astar_pathfinding(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_abs_val(agam_int __p0) {
  agam_int _local_v = __p0;
block_2:
  agam_int __v4 = _local_v;
  agam_int __v6 = 0;
  agam_bool __v7 = __v4 < __v6;
  if (__v7) goto block_3; else goto block_5;
block_3:
  agam_int __v8 = _local_v;
  agam_int __v9 = -__v8;
  return __v9;
block_5:
  agam_int __v10 = _local_v;
  return __v10;
}

agam_int agam_astar_pathfinding(agam_int __p0) {
  agam_int _local_grid_size = __p0;
block_8:
  agam_int _local_max_nodes = 0;
  agam_int __v13 = 0;
  agam_int __v15 = 10000;
  _local_max_nodes = __v15;
  agam_int __v16 = __v15;
  agam_int _local_checksum = 0;
  agam_int __v17 = 0;
  agam_int __v19 = 0;
  _local_checksum = __v19;
  agam_int __v20 = __v19;
  agam_int _local_path_finds = 0;
  agam_int __v21 = 0;
  agam_int __v23 = 0;
  _local_path_finds = __v23;
  agam_int __v24 = __v23;
  goto block_9;
block_9:
  agam_int __v25 = _local_path_finds;
  agam_int __v27 = 100;
  agam_bool __v28 = __v25 < __v27;
  if (__v28) goto block_10; else goto block_11;
block_10:
  agam_int _local_start_x = 0;
  agam_int __v29 = 0;
  agam_int __v30 = _local_path_finds;
  agam_int __v32 = 17;
  agam_int __v33 = __v30 * __v32;
  agam_int __v34 = _local_grid_size;
  agam_int __v35 = __v33 % __v34;
  _local_start_x = __v35;
  agam_int __v36 = __v35;
  agam_int _local_start_y = 0;
  agam_int __v37 = 0;
  agam_int __v38 = _local_path_finds;
  agam_int __v40 = 19;
  agam_int __v41 = __v38 * __v40;
  agam_int __v42 = _local_grid_size;
  agam_int __v43 = __v41 % __v42;
  _local_start_y = __v43;
  agam_int __v44 = __v43;
  agam_int _local_goal_x = 0;
  agam_int __v45 = 0;
  agam_int __v46 = _local_grid_size;
  agam_int __v48 = 1;
  agam_int __v49 = __v46 - __v48;
  agam_int __v50 = _local_path_finds;
  agam_int __v52 = 23;
  agam_int __v53 = __v50 * __v52;
  agam_int __v54 = _local_grid_size;
  agam_int __v55 = __v53 % __v54;
  agam_int __v56 = __v49 - __v55;
  _local_goal_x = __v56;
  agam_int __v57 = __v56;
  agam_int _local_goal_y = 0;
  agam_int __v58 = 0;
  agam_int __v59 = _local_grid_size;
  agam_int __v61 = 1;
  agam_int __v62 = __v59 - __v61;
  agam_int __v63 = _local_path_finds;
  agam_int __v65 = 29;
  agam_int __v66 = __v63 * __v65;
  agam_int __v67 = _local_grid_size;
  agam_int __v68 = __v66 % __v67;
  agam_int __v69 = __v62 - __v68;
  _local_goal_y = __v69;
  agam_int __v70 = __v69;
  agam_int _local_nodes_expanded = 0;
  agam_int __v71 = 0;
  agam_int __v73 = 0;
  _local_nodes_expanded = __v73;
  agam_int __v74 = __v73;
  agam_int _local_curr_x = 0;
  agam_int __v75 = 0;
  agam_int __v76 = _local_start_x;
  _local_curr_x = __v76;
  agam_int __v77 = __v76;
  agam_int _local_curr_y = 0;
  agam_int __v78 = 0;
  agam_int __v79 = _local_start_y;
  _local_curr_y = __v79;
  agam_int __v80 = __v79;
  agam_int _local_path_len = 0;
  agam_int __v81 = 0;
  agam_int __v83 = 0;
  _local_path_len = __v83;
  agam_int __v84 = __v83;
  goto block_12;
block_12:
  agam_int __v85 = _local_curr_x;
  agam_int __v86 = _local_goal_x;
  agam_bool __v87 = __v85 != __v86;
  agam_int __v88 = _local_curr_y;
  agam_int __v89 = _local_goal_y;
  agam_bool __v90 = __v88 != __v89;
  agam_bool __v91 = __v87 || __v90;
  agam_int __v92 = _local_nodes_expanded;
  agam_int __v93 = _local_max_nodes;
  agam_bool __v94 = __v92 < __v93;
  agam_bool __v95 = __v91 && __v94;
  if (__v95) goto block_13; else goto block_14;
block_13:
  agam_int __v96 = _local_nodes_expanded;
  agam_int __v98 = 1;
  agam_int __v99 = __v96 + __v98;
  _local_nodes_expanded = __v99;
  agam_int __v100 = __v99;
  agam_int _local_best_next_x = 0;
  agam_int __v101 = 0;
  agam_int __v102 = _local_curr_x;
  _local_best_next_x = __v102;
  agam_int __v103 = __v102;
  agam_int _local_best_next_y = 0;
  agam_int __v104 = 0;
  agam_int __v105 = _local_curr_y;
  _local_best_next_y = __v105;
  agam_int __v106 = __v105;
  agam_int _local_best_f = 0;
  agam_int __v107 = 0;
  agam_int __v109 = 999999999;
  _local_best_f = __v109;
  agam_int __v110 = __v109;
  agam_int _local_dy = 0;
  agam_int __v111 = 0;
  agam_int __v114 = -1;
  _local_dy = __v114;
  agam_int __v115 = __v114;
  goto block_15;
block_15:
  agam_int __v116 = _local_dy;
  agam_int __v118 = 1;
  agam_bool __v119 = __v116 <= __v118;
  if (__v119) goto block_16; else goto block_17;
block_16:
  agam_int _local_dx = 0;
  agam_int __v120 = 0;
  agam_int __v123 = -1;
  _local_dx = __v123;
  agam_int __v124 = __v123;
  goto block_18;
block_18:
  agam_int __v125 = _local_dx;
  agam_int __v127 = 1;
  agam_bool __v128 = __v125 <= __v127;
  if (__v128) goto block_19; else goto block_20;
block_19:
  agam_int __v129 = _local_dx;
  agam_int __v131 = 0;
  agam_bool __v132 = __v129 != __v131;
  agam_int __v133 = _local_dy;
  agam_int __v135 = 0;
  agam_bool __v136 = __v133 != __v135;
  agam_bool __v137 = __v132 || __v136;
  if (__v137) goto block_21; else goto block_23;
block_21:
  agam_int _local_nx = 0;
  agam_int __v138 = 0;
  agam_int __v139 = _local_curr_x;
  agam_int __v140 = _local_dx;
  agam_int __v141 = __v139 + __v140;
  _local_nx = __v141;
  agam_int __v142 = __v141;
  agam_int _local_ny = 0;
  agam_int __v143 = 0;
  agam_int __v144 = _local_curr_y;
  agam_int __v145 = _local_dy;
  agam_int __v146 = __v144 + __v145;
  _local_ny = __v146;
  agam_int __v147 = __v146;
  agam_int __v148 = _local_nx;
  agam_int __v150 = 0;
  agam_bool __v151 = __v148 >= __v150;
  agam_int __v152 = _local_nx;
  agam_int __v153 = _local_grid_size;
  agam_bool __v154 = __v152 < __v153;
  agam_bool __v155 = __v151 && __v154;
  agam_int __v156 = _local_ny;
  agam_int __v158 = 0;
  agam_bool __v159 = __v156 >= __v158;
  agam_bool __v160 = __v155 && __v159;
  agam_int __v161 = _local_ny;
  agam_int __v162 = _local_grid_size;
  agam_bool __v163 = __v161 < __v162;
  agam_bool __v164 = __v160 && __v163;
  if (__v164) goto block_24; else goto block_26;
block_24:
  agam_int _local_is_obstacle = 0;
  agam_int __v165 = 0;
  agam_int __v166 = _local_nx;
  agam_int __v168 = 31;
  agam_int __v169 = __v166 * __v168;
  agam_int __v170 = _local_ny;
  agam_int __v172 = 37;
  agam_int __v173 = __v170 * __v172;
  agam_int __v174 = __v169 + __v173;
  agam_int __v176 = 100;
  agam_int __v177 = __v174 % __v176;
  agam_int __v179 = 20;
  agam_bool __v180 = __v177 < __v179;
  _local_is_obstacle = __v180;
  agam_int __v181 = __v180;
  agam_int __v182 = _local_is_obstacle;
  agam_int __v184 = 0;
  agam_bool __v185 = __v182 == __v184;
  if (__v185) goto block_27; else goto block_29;
block_27:
  agam_int _local_g = 0;
  agam_int __v186 = 0;
  agam_int __v187 = _local_path_len;
  agam_int __v189 = 10;
  agam_int __v190 = __v187 + __v189;
  _local_g = __v190;
  agam_int __v191 = __v190;
  agam_int __v192 = _local_dx;
  agam_int __v194 = 0;
  agam_bool __v195 = __v192 != __v194;
  agam_int __v196 = _local_dy;
  agam_int __v198 = 0;
  agam_bool __v199 = __v196 != __v198;
  agam_bool __v200 = __v195 && __v199;
  if (__v200) goto block_30; else goto block_32;
block_30:
  agam_int __v201 = _local_g;
  agam_int __v203 = 4;
  agam_int __v204 = __v201 + __v203;
  _local_g = __v204;
  agam_int __v205 = __v204;
  goto block_32;
block_32:
  agam_int _local_h = 0;
  agam_int __v206 = 0;
  agam_int __v207 = _local_nx;
  agam_int __v208 = _local_goal_x;
  agam_int __v209 = __v207 - __v208;
  agam_int __v210 = agam_abs_val(__v209);
  agam_int __v211 = _local_ny;
  agam_int __v212 = _local_goal_y;
  agam_int __v213 = __v211 - __v212;
  agam_int __v214 = agam_abs_val(__v213);
  agam_int __v215 = __v210 + __v214;
  agam_int __v217 = 10;
  agam_int __v218 = __v215 * __v217;
  _local_h = __v218;
  agam_int __v219 = __v218;
  agam_int _local_f = 0;
  agam_int __v220 = 0;
  agam_int __v221 = _local_g;
  agam_int __v222 = _local_h;
  agam_int __v223 = __v221 + __v222;
  _local_f = __v223;
  agam_int __v224 = __v223;
  agam_int __v225 = _local_f;
  agam_int __v226 = _local_best_f;
  agam_bool __v227 = __v225 < __v226;
  if (__v227) goto block_33; else goto block_35;
block_33:
  agam_int __v228 = _local_f;
  _local_best_f = __v228;
  agam_int __v229 = __v228;
  agam_int __v230 = _local_nx;
  _local_best_next_x = __v230;
  agam_int __v231 = __v230;
  agam_int __v232 = _local_ny;
  _local_best_next_y = __v232;
  agam_int __v233 = __v232;
  goto block_35;
block_35:
  goto block_29;
block_29:
  goto block_26;
block_26:
  goto block_23;
block_23:
  agam_int __v234 = _local_dx;
  agam_int __v236 = 1;
  agam_int __v237 = __v234 + __v236;
  _local_dx = __v237;
  agam_int __v238 = __v237;
  goto block_18;
block_20:
  agam_int __v239 = _local_dy;
  agam_int __v241 = 1;
  agam_int __v242 = __v239 + __v241;
  _local_dy = __v242;
  agam_int __v243 = __v242;
  goto block_15;
block_17:
  agam_int __v244 = _local_best_next_x;
  agam_int __v245 = _local_curr_x;
  agam_bool __v246 = __v244 == __v245;
  agam_int __v247 = _local_best_next_y;
  agam_int __v248 = _local_curr_y;
  agam_bool __v249 = __v247 == __v248;
  agam_bool __v250 = __v246 && __v249;
  if (__v250) goto block_36; else goto block_37;
block_36:
  agam_int __v251 = _local_max_nodes;
  _local_nodes_expanded = __v251;
  agam_int __v252 = __v251;
  goto block_38;
block_37:
  agam_int __v253 = _local_best_next_x;
  _local_curr_x = __v253;
  agam_int __v254 = __v253;
  agam_int __v255 = _local_best_next_y;
  _local_curr_y = __v255;
  agam_int __v256 = __v255;
  agam_int __v257 = _local_path_len;
  agam_int __v259 = 10;
  agam_int __v260 = __v257 + __v259;
  _local_path_len = __v260;
  agam_int __v261 = __v260;
  goto block_38;
block_38:
  goto block_12;
block_14:
  agam_int __v262 = _local_checksum;
  agam_int __v264 = 31;
  agam_int __v265 = __v262 * __v264;
  agam_int __v266 = _local_path_len;
  agam_int __v267 = __v265 + __v266;
  agam_int __v268 = _local_nodes_expanded;
  agam_int __v269 = __v267 + __v268;
  agam_int __v271 = 1000000007;
  agam_int __v272 = __v269 % __v271;
  _local_checksum = __v272;
  agam_int __v273 = __v272;
  agam_int __v274 = _local_path_finds;
  agam_int __v276 = 1;
  agam_int __v277 = __v274 + __v276;
  _local_path_finds = __v277;
  agam_int __v278 = __v277;
  goto block_9;
block_11:
  agam_int __v279 = _local_checksum;
  return __v279;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_40:
  agam_int __v282 = 100;
  agam_int __v283 = agam_astar_pathfinding(__v282);
  printf("%lld", (long long)__v283);
  printf("\n");
  agam_int __v284 = 0;
  agam_int __v285 = 0;
  return (int)__v285;
}

