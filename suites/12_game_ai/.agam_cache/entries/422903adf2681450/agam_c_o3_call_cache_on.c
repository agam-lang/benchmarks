/* Generated by agamc — Agam Compiler */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include <math.h>
#include <time.h>

typedef int64_t agam_int;
typedef double agam_float;
typedef int agam_bool;
typedef const char* agam_str;

/* ── Agam Runtime Prelude ──────────────────── */
agam_int agam_println(agam_str s) { printf("%s\n", s); return 0; }
agam_int agam_print(agam_str s) { printf("%s", s); return 0; }
agam_float agam_clock(void) { return (agam_float)clock() / (agam_float)CLOCKS_PER_SEC; }
static agam_int agam_runtime_argc = 0;
static char** agam_runtime_argv = NULL;
agam_int agam_argc(void) { return agam_runtime_argc; }
agam_str agam_argv(agam_int index) {
  if (!agam_runtime_argv || index < 0 || index >= agam_runtime_argc) {
    return "";
  }
  return agam_runtime_argv[index];
}
agam_int agam_parse_int(agam_str s) {
  if (!s) {
    return 0;
  }
  return (agam_int)strtoll(s, NULL, 10);
}

agam_str agam_str_concat(agam_str a, agam_str b) {
  size_t a_len = strlen(a);
  size_t b_len = strlen(b);
  char* out = (char*)malloc(a_len + b_len + 1);
  memcpy(out, a, a_len);
  memcpy(out + a_len, b, b_len + 1);
  return out;
}

typedef struct AgamTensor {
  agam_int rows;
  agam_int cols;
  agam_int len;
  agam_float* data;
} AgamTensor;

typedef struct AgamDataFrame {
  agam_int len;
  agam_int* ids;
  agam_int* groups;
  agam_float* scores;
} AgamDataFrame;

typedef struct AgamRow {
  agam_int id;
  agam_int group;
  agam_float score;
} AgamRow;

static uint64_t agam_mix64(uint64_t x) {
  x ^= x >> 33;
  x *= 0xff51afd7ed558ccdULL;
  x ^= x >> 33;
  x *= 0xc4ceb9fe1a85ec53ULL;
  x ^= x >> 33;
  return x;
}

static uint64_t agam_seed_bits(agam_float seed) {
  union {
    agam_float f;
    uint64_t u;
  } bits;
  bits.f = seed;
  return bits.u;
}

static agam_float agam_hash_unit(agam_int index, agam_float seed, agam_int salt) {
  uint64_t mixed = agam_mix64((uint64_t)index ^ agam_seed_bits(seed) ^ ((uint64_t)salt << 32));
  return (agam_float)((mixed >> 11) * (1.0 / 9007199254740992.0));
}

static agam_float agam_weight_sample(agam_int row, agam_int col, agam_float seed) {
  return agam_hash_unit(row * 4099 + col * 131, seed, 17) * 2.0 - 1.0;
}

static agam_float agam_bias_sample(agam_int index, agam_float seed) {
  return agam_hash_unit(index * 7919, seed, 29) * 0.25 - 0.125;
}

static AgamTensor* agam_tensor_new(agam_int rows, agam_int cols) {
  AgamTensor* tensor = (AgamTensor*)malloc(sizeof(AgamTensor));
  tensor->rows = rows;
  tensor->cols = cols;
  tensor->len = rows * cols;
  tensor->data = tensor->len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)tensor->len) : NULL;
  return tensor;
}

static AgamDataFrame* agam_dataframe_new(agam_int len) {
  AgamDataFrame* df = (AgamDataFrame*)malloc(sizeof(AgamDataFrame));
  df->len = len;
  df->ids = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->groups = len > 0 ? (agam_int*)malloc(sizeof(agam_int) * (size_t)len) : NULL;
  df->scores = len > 0 ? (agam_float*)malloc(sizeof(agam_float) * (size_t)len) : NULL;
  return df;
}

static int agam_compare_rows_by_score_desc(const void* left, const void* right) {
  const AgamRow* a = (const AgamRow*)left;
  const AgamRow* b = (const AgamRow*)right;
  if (a->score < b->score) {
    return 1;
  }
  if (a->score > b->score) {
    return -1;
  }
  return 0;
}

/* ── Agam GPU Host Runtime ─────────────────── */
void* agam_gpu_malloc(agam_int size_bytes) {
  if (size_bytes <= 0) {
    return NULL;
  }
  return malloc((size_t)size_bytes);
}

agam_int agam_gpu_free(void* ptr) {
  free(ptr);
  return 0;
}

agam_int agam_gpu_memcpy_to_device(void* device_dst, void* host_src, agam_int size_bytes) {
  if (!device_dst || !host_src || size_bytes < 0) {
    return -1;
  }
  memcpy(device_dst, host_src, (size_t)size_bytes);
  return 0;
}

agam_int agam_gpu_memcpy_to_host(void* host_dst, void* device_src, agam_int size_bytes) {
  if (!host_dst || !device_src || size_bytes < 0) {
    return -1;
  }
  memcpy(host_dst, device_src, (size_t)size_bytes);
  return 0;
}

agam_float agam_adam(agam_float x0, agam_float y0, agam_float learning_rate, agam_int max_iter, agam_float tol) {
  agam_float x = x0;
  agam_float y = y0;
  agam_float mx = 0.0;
  agam_float my = 0.0;
  agam_float vx = 0.0;
  agam_float vy = 0.0;
  const agam_float beta1 = 0.9;
  const agam_float beta2 = 0.999;
  const agam_float epsilon = 1e-8;

  for (agam_int t = 1; t <= max_iter; ++t) {
    agam_float dx = -2.0 * (1.0 - x) - 400.0 * x * (y - x * x);
    agam_float dy = 200.0 * (y - x * x);
    agam_float grad_norm = sqrt(dx * dx + dy * dy);
    if (grad_norm < tol) {
      break;
    }

    mx = beta1 * mx + (1.0 - beta1) * dx;
    my = beta1 * my + (1.0 - beta1) * dy;
    vx = beta2 * vx + (1.0 - beta2) * dx * dx;
    vy = beta2 * vy + (1.0 - beta2) * dy * dy;

    agam_float t_f = (agam_float)t;
    agam_float mx_hat = mx / (1.0 - pow(beta1, t_f));
    agam_float my_hat = my / (1.0 - pow(beta1, t_f));
    agam_float vx_hat = vx / (1.0 - pow(beta2, t_f));
    agam_float vy_hat = vy / (1.0 - pow(beta2, t_f));

    x -= learning_rate * mx_hat / (sqrt(vx_hat) + epsilon);
    y -= learning_rate * my_hat / (sqrt(vy_hat) + epsilon);
  }

  {
    agam_float a = 1.0 - x;
    agam_float b = y - x * x;
    return a * a + 100.0 * b * b;
  }
}

AgamDataFrame* agam_dataframe_build_sin(agam_int rows) {
  AgamDataFrame* df = agam_dataframe_new(rows);
  for (agam_int i = 0; i < rows; ++i) {
    df->ids[i] = i;
    df->groups[i] = i % 1024;
    df->scores[i] = sin((agam_float)i * 0.1);
  }
  return df;
}

AgamDataFrame* agam_dataframe_filter_gt(AgamDataFrame* df, agam_float threshold) {
  if (!df) {
    return NULL;
  }

  agam_int count = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      ++count;
    }
  }

  AgamDataFrame* filtered = agam_dataframe_new(count);
  agam_int out_index = 0;
  for (agam_int i = 0; i < df->len; ++i) {
    if (df->scores[i] > threshold) {
      filtered->ids[out_index] = df->ids[i];
      filtered->groups[out_index] = df->groups[i];
      filtered->scores[out_index] = df->scores[i];
      ++out_index;
    }
  }
  return filtered;
}

AgamDataFrame* agam_dataframe_sort(AgamDataFrame* df) {
  if (!df) {
    return NULL;
  }

  AgamRow* rows = df->len > 0 ? (AgamRow*)malloc(sizeof(AgamRow) * (size_t)df->len) : NULL;
  for (agam_int i = 0; i < df->len; ++i) {
    rows[i].id = df->ids[i];
    rows[i].group = df->groups[i];
    rows[i].score = df->scores[i];
  }

  qsort(rows, (size_t)df->len, sizeof(AgamRow), agam_compare_rows_by_score_desc);

  AgamDataFrame* sorted = agam_dataframe_new(df->len);
  for (agam_int i = 0; i < df->len; ++i) {
    sorted->ids[i] = rows[i].id;
    sorted->groups[i] = rows[i].group;
    sorted->scores[i] = rows[i].score;
  }

  free(rows);
  return sorted;
}

AgamDataFrame* agam_dataframe_group_by(AgamDataFrame* df, agam_int group_count) {
  if (!df) {
    return NULL;
  }
  if (group_count <= 0) {
    group_count = 1;
  }

  agam_float* sums = (agam_float*)calloc((size_t)group_count, sizeof(agam_float));
  agam_int* counts = (agam_int*)calloc((size_t)group_count, sizeof(agam_int));

  for (agam_int i = 0; i < df->len; ++i) {
    agam_int bucket = df->groups[i] % group_count;
    if (bucket < 0) {
      bucket += group_count;
    }
    sums[bucket] += df->scores[i];
    counts[bucket] += 1;
  }

  agam_int used = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      ++used;
    }
  }

  AgamDataFrame* grouped = agam_dataframe_new(used);
  agam_int out_index = 0;
  for (agam_int i = 0; i < group_count; ++i) {
    if (counts[i] > 0) {
      grouped->ids[out_index] = i;
      grouped->groups[out_index] = i;
      grouped->scores[out_index] = sums[i] / (agam_float)counts[i];
      ++out_index;
    }
  }

  free(sums);
  free(counts);
  return grouped;
}

agam_float agam_dataframe_mean(AgamDataFrame* df) {
  if (!df || df->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < df->len; ++i) {
    sum += df->scores[i];
  }
  return sum / (agam_float)df->len;
}

agam_int agam_dataframe_free(AgamDataFrame* df) {
  if (!df) {
    return 0;
  }
  free(df->ids);
  free(df->groups);
  free(df->scores);
  free(df);
  return 0;
}

AgamTensor* agam_tensor_fill_rand(agam_int rows, agam_int cols, agam_float seed) {
  AgamTensor* tensor = agam_tensor_new(rows, cols);
  for (agam_int i = 0; i < tensor->len; ++i) {
    tensor->data[i] = agam_hash_unit(i, seed, 43) * 2.0 - 1.0;
  }
  return tensor;
}

AgamTensor* agam_dense_layer(AgamTensor* input, agam_int out_features, agam_float seed) {
  if (!input || out_features <= 0) {
    return NULL;
  }

  AgamTensor* output = agam_tensor_new(input->rows, out_features);
  for (agam_int row = 0; row < input->rows; ++row) {
    for (agam_int col = 0; col < out_features; ++col) {
      agam_float acc = agam_bias_sample(col, seed);
      for (agam_int inner = 0; inner < input->cols; ++inner) {
        agam_float weight = agam_weight_sample(inner, col, seed);
        acc += input->data[row * input->cols + inner] * weight;
      }
      output->data[row * out_features + col] = acc > 0.0 ? acc : 0.0;
    }
  }
  return output;
}

AgamTensor* agam_conv2d(AgamTensor* input, agam_int kernel_size, agam_float seed) {
  if (!input || kernel_size <= 0 || input->rows < kernel_size || input->cols < kernel_size) {
    return NULL;
  }

  agam_int out_rows = input->rows - kernel_size + 1;
  agam_int out_cols = input->cols - kernel_size + 1;
  AgamTensor* output = agam_tensor_new(out_rows, out_cols);

  for (agam_int y = 0; y < out_rows; ++y) {
    for (agam_int x = 0; x < out_cols; ++x) {
      agam_float acc = 0.0;
      for (agam_int ky = 0; ky < kernel_size; ++ky) {
        for (agam_int kx = 0; kx < kernel_size; ++kx) {
          agam_float kernel = agam_weight_sample(ky, kx, seed);
          agam_float value = input->data[(y + ky) * input->cols + (x + kx)];
          acc += value * kernel;
        }
      }
      output->data[y * out_cols + x] = acc;
    }
  }
  return output;
}

agam_float agam_tensor_checksum(AgamTensor* tensor) {
  if (!tensor || tensor->len == 0) {
    return 0.0;
  }

  agam_float sum = 0.0;
  for (agam_int i = 0; i < tensor->len; ++i) {
    sum += tensor->data[i] * (1.0 + (agam_float)(i & 7));
  }
  return sum;
}

agam_int agam_tensor_free(AgamTensor* tensor) {
  if (!tensor) {
    return 0;
  }
  free(tensor->data);
  free(tensor);
  return 0;
}

/* ── Agam Effect Runtime ───────────────────── */
#include <sys/stat.h>
#include <errno.h>

/* FileSystem.exists(path) -> bool */
agam_bool agam_effect_FileSystem_exists(agam_str path) {
  struct stat st;
  return stat(path, &st) == 0 ? 1 : 0;
}

/* FileSystem.is_file(path) -> bool */
agam_bool agam_effect_FileSystem_is_file(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISREG(st.st_mode) ? 1 : 0;
}

/* FileSystem.is_dir(path) -> bool */
agam_bool agam_effect_FileSystem_is_dir(agam_str path) {
  struct stat st;
  if (stat(path, &st) != 0) return 0;
  return S_ISDIR(st.st_mode) ? 1 : 0;
}

/* FileSystem.create_dir_all(path) */
agam_int agam_effect_FileSystem_create_dir_all(agam_str path) {
  /* Simple recursive mkdir for POSIX; on Windows use _mkdir */
#ifdef _WIN32
  (void)_mkdir(path);
#else
  mkdir(path, 0755);
#endif
  return 0;
}

/* FileSystem.read_to_string(path) -> string */
agam_str agam_effect_FileSystem_read_to_string(agam_str path) {
  FILE* f = fopen(path, "rb");
  if (!f) return "";
  fseek(f, 0, SEEK_END);
  long len = ftell(f);
  fseek(f, 0, SEEK_SET);
  char* buf = (char*)malloc((size_t)len + 1);
  if (len > 0) {
    size_t read = fread(buf, 1, (size_t)len, f);
    buf[read] = '\0';
  } else {
    buf[0] = '\0';
  }
  fclose(f);
  return buf;
}

/* FileSystem.read_lines(path) -> string (newline-joined) */
agam_str agam_effect_FileSystem_read_lines(agam_str path) {
  return agam_effect_FileSystem_read_to_string(path);
}

/* FileSystem.write_string(path, contents) */
agam_int agam_effect_FileSystem_write_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "w");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.append_string(path, contents) */
agam_int agam_effect_FileSystem_append_string(agam_str path, agam_str contents) {
  FILE* f = fopen(path, "a");
  if (!f) return -1;
  fputs(contents, f);
  fclose(f);
  return 0;
}

/* FileSystem.list_dir(path) -> string (newline-joined entries) */
agam_str agam_effect_FileSystem_list_dir(agam_str path) {
  (void)path;
  return ""; /* simplified: full implementation requires dirent.h */
}

/* Console.print(msg) */
agam_int agam_effect_Console_print(agam_str msg) {
  printf("%s", msg);
  return 0;
}

/* Console.println(msg) */
agam_int agam_effect_Console_println(agam_str msg) {
  printf("%s\n", msg);
  return 0;
}

/* Console.read_line() -> string */
agam_str agam_effect_Console_read_line(void) {
  char* buf = (char*)malloc(4096);
  if (!fgets(buf, 4096, stdin)) {
    buf[0] = '\0';
  }
  /* Strip trailing newline */
  size_t len = strlen(buf);
  if (len > 0 && buf[len-1] == '\n') buf[len-1] = '\0';
  return buf;
}

/* Console.eprint(msg) */
agam_int agam_effect_Console_eprint(agam_str msg) {
  fprintf(stderr, "%s", msg);
  return 0;
}

/* Console.eprintln(msg) */
agam_int agam_effect_Console_eprintln(agam_str msg) {
  fprintf(stderr, "%s\n", msg);
  return 0;
}


agam_int agam_i64_const(agam_int __p0);
agam_int agam_flocking_boids(agam_int __p0);
int main(int argc, char** argv);

agam_int agam_i64_const(agam_int __p0) {
  agam_int _local_value = __p0;
block_0:
  agam_int __v1 = _local_value;
  return __v1;
}

agam_int agam_flocking_boids(agam_int __p0) {
  agam_int _local_boids = __p0;
block_2:
  agam_int _local_checksum = 0;
  agam_int __v4 = 0;
  agam_int __v6 = 0;
  _local_checksum = __v6;
  agam_int __v7 = __v6;
  agam_int _local_frames = 0;
  agam_int __v8 = 0;
  agam_int __v10 = 30;
  _local_frames = __v10;
  agam_int __v11 = __v10;
  agam_int _local_f = 0;
  agam_int __v12 = 0;
  agam_int __v14 = 0;
  _local_f = __v14;
  agam_int __v15 = __v14;
  goto block_3;
block_3:
  agam_int __v16 = _local_f;
  agam_int __v17 = _local_frames;
  agam_bool __v18 = __v16 < __v17;
  if (__v18) goto block_4; else goto block_5;
block_4:
  agam_int _local_center_mass_sum = 0;
  agam_int __v19 = 0;
  agam_int __v21 = 0;
  _local_center_mass_sum = __v21;
  agam_int __v22 = __v21;
  agam_int _local_i = 0;
  agam_int __v23 = 0;
  agam_int __v25 = 0;
  _local_i = __v25;
  agam_int __v26 = __v25;
  goto block_6;
block_6:
  agam_int __v27 = _local_i;
  agam_int __v28 = _local_boids;
  agam_bool __v29 = __v27 < __v28;
  if (__v29) goto block_7; else goto block_8;
block_7:
  agam_int _local_px = 0;
  agam_int __v30 = 0;
  agam_int __v31 = _local_i;
  agam_int __v33 = 17;
  agam_int __v34 = __v31 * __v33;
  agam_int __v35 = _local_f;
  agam_int __v37 = 7;
  agam_int __v38 = __v35 * __v37;
  agam_int __v39 = __v34 + __v38;
  agam_int __v41 = 1000;
  agam_int __v42 = __v39 % __v41;
  _local_px = __v42;
  agam_int __v43 = __v42;
  agam_int _local_py = 0;
  agam_int __v44 = 0;
  agam_int __v45 = _local_i;
  agam_int __v47 = 19;
  agam_int __v48 = __v45 * __v47;
  agam_int __v49 = _local_f;
  agam_int __v51 = 11;
  agam_int __v52 = __v49 * __v51;
  agam_int __v53 = __v48 + __v52;
  agam_int __v55 = 1000;
  agam_int __v56 = __v53 % __v55;
  _local_py = __v56;
  agam_int __v57 = __v56;
  agam_int _local_vx = 0;
  agam_int __v58 = 0;
  agam_int __v59 = _local_i;
  agam_int __v61 = 23;
  agam_int __v62 = __v59 * __v61;
  agam_int __v64 = 20;
  agam_int __v65 = __v62 % __v64;
  agam_int __v67 = 10;
  agam_int __v68 = __v65 - __v67;
  _local_vx = __v68;
  agam_int __v69 = __v68;
  agam_int _local_vy = 0;
  agam_int __v70 = 0;
  agam_int __v71 = _local_i;
  agam_int __v73 = 29;
  agam_int __v74 = __v71 * __v73;
  agam_int __v76 = 20;
  agam_int __v77 = __v74 % __v76;
  agam_int __v79 = 10;
  agam_int __v80 = __v77 - __v79;
  _local_vy = __v80;
  agam_int __v81 = __v80;
  agam_int _local_sep_x = 0;
  agam_int __v82 = 0;
  agam_int __v84 = 0;
  _local_sep_x = __v84;
  agam_int __v85 = __v84;
  agam_int _local_sep_y = 0;
  agam_int __v86 = 0;
  agam_int __v88 = 0;
  _local_sep_y = __v88;
  agam_int __v89 = __v88;
  agam_int _local_align_x = 0;
  agam_int __v90 = 0;
  agam_int __v92 = 0;
  _local_align_x = __v92;
  agam_int __v93 = __v92;
  agam_int _local_align_y = 0;
  agam_int __v94 = 0;
  agam_int __v96 = 0;
  _local_align_y = __v96;
  agam_int __v97 = __v96;
  agam_int _local_coh_x = 0;
  agam_int __v98 = 0;
  agam_int __v100 = 0;
  _local_coh_x = __v100;
  agam_int __v101 = __v100;
  agam_int _local_coh_y = 0;
  agam_int __v102 = 0;
  agam_int __v104 = 0;
  _local_coh_y = __v104;
  agam_int __v105 = __v104;
  agam_int _local_neighbors = 0;
  agam_int __v106 = 0;
  agam_int __v108 = 0;
  _local_neighbors = __v108;
  agam_int __v109 = __v108;
  agam_int _local_j = 0;
  agam_int __v110 = 0;
  agam_int __v112 = 0;
  _local_j = __v112;
  agam_int __v113 = __v112;
  goto block_9;
block_9:
  agam_int __v114 = _local_j;
  agam_int __v115 = _local_boids;
  agam_bool __v116 = __v114 < __v115;
  if (__v116) goto block_10; else goto block_11;
block_10:
  agam_int __v117 = _local_i;
  agam_int __v118 = _local_j;
  agam_bool __v119 = __v117 != __v118;
  if (__v119) goto block_12; else goto block_14;
block_12:
  agam_int _local_ox = 0;
  agam_int __v120 = 0;
  agam_int __v121 = _local_j;
  agam_int __v123 = 17;
  agam_int __v124 = __v121 * __v123;
  agam_int __v125 = _local_f;
  agam_int __v127 = 7;
  agam_int __v128 = __v125 * __v127;
  agam_int __v129 = __v124 + __v128;
  agam_int __v131 = 1000;
  agam_int __v132 = __v129 % __v131;
  _local_ox = __v132;
  agam_int __v133 = __v132;
  agam_int _local_oy = 0;
  agam_int __v134 = 0;
  agam_int __v135 = _local_j;
  agam_int __v137 = 19;
  agam_int __v138 = __v135 * __v137;
  agam_int __v139 = _local_f;
  agam_int __v141 = 11;
  agam_int __v142 = __v139 * __v141;
  agam_int __v143 = __v138 + __v142;
  agam_int __v145 = 1000;
  agam_int __v146 = __v143 % __v145;
  _local_oy = __v146;
  agam_int __v147 = __v146;
  agam_int _local_ovx = 0;
  agam_int __v148 = 0;
  agam_int __v149 = _local_j;
  agam_int __v151 = 23;
  agam_int __v152 = __v149 * __v151;
  agam_int __v154 = 20;
  agam_int __v155 = __v152 % __v154;
  agam_int __v157 = 10;
  agam_int __v158 = __v155 - __v157;
  _local_ovx = __v158;
  agam_int __v159 = __v158;
  agam_int _local_ovy = 0;
  agam_int __v160 = 0;
  agam_int __v161 = _local_j;
  agam_int __v163 = 29;
  agam_int __v164 = __v161 * __v163;
  agam_int __v166 = 20;
  agam_int __v167 = __v164 % __v166;
  agam_int __v169 = 10;
  agam_int __v170 = __v167 - __v169;
  _local_ovy = __v170;
  agam_int __v171 = __v170;
  agam_int _local_dx = 0;
  agam_int __v172 = 0;
  agam_int __v173 = _local_px;
  agam_int __v174 = _local_ox;
  agam_int __v175 = __v173 - __v174;
  _local_dx = __v175;
  agam_int __v176 = __v175;
  agam_int _local_dy = 0;
  agam_int __v177 = 0;
  agam_int __v178 = _local_py;
  agam_int __v179 = _local_oy;
  agam_int __v180 = __v178 - __v179;
  _local_dy = __v180;
  agam_int __v181 = __v180;
  agam_int _local_dist_sq = 0;
  agam_int __v182 = 0;
  agam_int __v183 = _local_dx;
  agam_int __v184 = _local_dx;
  agam_int __v185 = __v183 * __v184;
  agam_int __v186 = _local_dy;
  agam_int __v187 = _local_dy;
  agam_int __v188 = __v186 * __v187;
  agam_int __v189 = __v185 + __v188;
  _local_dist_sq = __v189;
  agam_int __v190 = __v189;
  agam_int __v191 = _local_dist_sq;
  agam_int __v193 = 2500;
  agam_bool __v194 = __v191 < __v193;
  if (__v194) goto block_15; else goto block_17;
block_15:
  agam_int __v195 = _local_neighbors;
  agam_int __v197 = 1;
  agam_int __v198 = __v195 + __v197;
  _local_neighbors = __v198;
  agam_int __v199 = __v198;
  agam_int __v200 = _local_dist_sq;
  agam_int __v202 = 400;
  agam_bool __v203 = __v200 < __v202;
  if (__v203) goto block_18; else goto block_20;
block_18:
  agam_int __v204 = _local_sep_x;
  agam_int __v205 = _local_dx;
  agam_int __v206 = __v204 + __v205;
  _local_sep_x = __v206;
  agam_int __v207 = __v206;
  agam_int __v208 = _local_sep_y;
  agam_int __v209 = _local_dy;
  agam_int __v210 = __v208 + __v209;
  _local_sep_y = __v210;
  agam_int __v211 = __v210;
  goto block_20;
block_20:
  agam_int __v212 = _local_align_x;
  agam_int __v213 = _local_ovx;
  agam_int __v214 = __v212 + __v213;
  _local_align_x = __v214;
  agam_int __v215 = __v214;
  agam_int __v216 = _local_align_y;
  agam_int __v217 = _local_ovy;
  agam_int __v218 = __v216 + __v217;
  _local_align_y = __v218;
  agam_int __v219 = __v218;
  agam_int __v220 = _local_coh_x;
  agam_int __v221 = _local_ox;
  agam_int __v222 = __v220 + __v221;
  _local_coh_x = __v222;
  agam_int __v223 = __v222;
  agam_int __v224 = _local_coh_y;
  agam_int __v225 = _local_oy;
  agam_int __v226 = __v224 + __v225;
  _local_coh_y = __v226;
  agam_int __v227 = __v226;
  goto block_17;
block_17:
  goto block_14;
block_14:
  agam_int __v228 = _local_j;
  agam_int __v230 = 1;
  agam_int __v231 = __v228 + __v230;
  _local_j = __v231;
  agam_int __v232 = __v231;
  goto block_9;
block_11:
  agam_int _local_new_vx = 0;
  agam_int __v233 = 0;
  agam_int __v234 = _local_vx;
  _local_new_vx = __v234;
  agam_int __v235 = __v234;
  agam_int _local_new_vy = 0;
  agam_int __v236 = 0;
  agam_int __v237 = _local_vy;
  _local_new_vy = __v237;
  agam_int __v238 = __v237;
  agam_int __v239 = _local_neighbors;
  agam_int __v241 = 0;
  agam_bool __v242 = __v239 > __v241;
  if (__v242) goto block_21; else goto block_23;
block_21:
  agam_int __v243 = _local_align_x;
  agam_int __v244 = _local_neighbors;
  agam_int __v245 = __v243 / __v244;
  agam_int __v246 = _local_vx;
  agam_int __v247 = __v245 - __v246;
  _local_align_x = __v247;
  agam_int __v248 = __v247;
  agam_int __v249 = _local_align_y;
  agam_int __v250 = _local_neighbors;
  agam_int __v251 = __v249 / __v250;
  agam_int __v252 = _local_vy;
  agam_int __v253 = __v251 - __v252;
  _local_align_y = __v253;
  agam_int __v254 = __v253;
  agam_int __v255 = _local_coh_x;
  agam_int __v256 = _local_neighbors;
  agam_int __v257 = __v255 / __v256;
  agam_int __v258 = _local_px;
  agam_int __v259 = __v257 - __v258;
  _local_coh_x = __v259;
  agam_int __v260 = __v259;
  agam_int __v261 = _local_coh_y;
  agam_int __v262 = _local_neighbors;
  agam_int __v263 = __v261 / __v262;
  agam_int __v264 = _local_py;
  agam_int __v265 = __v263 - __v264;
  _local_coh_y = __v265;
  agam_int __v266 = __v265;
  agam_int __v267 = _local_vx;
  agam_int __v268 = _local_sep_x;
  agam_int __v270 = 5;
  agam_int __v271 = __v268 * __v270;
  agam_int __v272 = _local_align_x;
  agam_int __v274 = 2;
  agam_int __v275 = __v272 * __v274;
  agam_int __v276 = __v271 + __v275;
  agam_int __v277 = _local_coh_x;
  agam_int __v279 = 1;
  agam_int __v280 = __v277 * __v279;
  agam_int __v281 = __v276 + __v280;
  agam_int __v283 = 100;
  agam_int __v284 = __v281 / __v283;
  agam_int __v285 = __v267 + __v284;
  _local_new_vx = __v285;
  agam_int __v286 = __v285;
  agam_int __v287 = _local_vy;
  agam_int __v288 = _local_sep_y;
  agam_int __v290 = 5;
  agam_int __v291 = __v288 * __v290;
  agam_int __v292 = _local_align_y;
  agam_int __v294 = 2;
  agam_int __v295 = __v292 * __v294;
  agam_int __v296 = __v291 + __v295;
  agam_int __v297 = _local_coh_y;
  agam_int __v299 = 1;
  agam_int __v300 = __v297 * __v299;
  agam_int __v301 = __v296 + __v300;
  agam_int __v303 = 100;
  agam_int __v304 = __v301 / __v303;
  agam_int __v305 = __v287 + __v304;
  _local_new_vy = __v305;
  agam_int __v306 = __v305;
  goto block_23;
block_23:
  agam_int __v307 = _local_center_mass_sum;
  agam_int __v308 = _local_new_vx;
  agam_int __v309 = __v307 + __v308;
  agam_int __v310 = _local_new_vy;
  agam_int __v311 = __v309 + __v310;
  agam_int __v313 = 1000000007;
  agam_int __v314 = __v311 % __v313;
  _local_center_mass_sum = __v314;
  agam_int __v315 = __v314;
  agam_int __v316 = _local_i;
  agam_int __v318 = 1;
  agam_int __v319 = __v316 + __v318;
  _local_i = __v319;
  agam_int __v320 = __v319;
  goto block_6;
block_8:
  agam_int __v321 = _local_checksum;
  agam_int __v323 = 31;
  agam_int __v324 = __v321 * __v323;
  agam_int __v325 = _local_center_mass_sum;
  agam_int __v326 = __v324 + __v325;
  agam_int __v328 = 1000000007;
  agam_int __v329 = __v326 % __v328;
  _local_checksum = __v329;
  agam_int __v330 = __v329;
  agam_int __v331 = _local_f;
  agam_int __v333 = 1;
  agam_int __v334 = __v331 + __v333;
  _local_f = __v334;
  agam_int __v335 = __v334;
  goto block_3;
block_5:
  agam_int __v336 = _local_checksum;
  return __v336;
}

int main(int argc, char** argv) {
  agam_runtime_argc = (agam_int)argc;
  agam_runtime_argv = argv;
block_25:
  agam_int __v339 = 512;
  agam_int __v340 = agam_flocking_boids(__v339);
  printf("%lld", (long long)__v340);
  printf("\n");
  agam_int __v341 = 0;
  agam_int __v342 = 0;
  return (int)__v342;
}

